# 静止した形の編集と目→口の案内 — 2026-09-08

## 2026-09-15 素材準備・Depth・表情の統合

[専用の検証記録](docs/material-integration-verification.md)に実データ、モデル実行、保存結果、自動テストと未検証範囲を記載。改修前の退避コミットは `36782f7`。

## 2026-09-15 カメラの顔向き追従

- MediaPipeの顔変換行列から左右・上下・首の傾きを取り出し、既存のyaw/pitch/headRollへ控えめに接続。距離や移動・拡大率は追従値に使用しない。正面基準、微小な揺れの除外、時間ベースの平滑化、上下限を追加。
- 出力画面に追従量（初期50%）、正面合わせ、顔向きの左右反転を追加。目の左右入れ替えとは独立。追従中は自動首振り・うなずきを重ねず、素材のheadPitch、髪揺れ・呼吸と音声優先の口パクを維持。
- Node全194件成功。追加4件で回転行列のXYZ/拡大率/移動、正面合わせ、微小揺れ、平滑化・制限、左右反転、0%、実captureモジュールへの模擬入力、顔喪失、停止時の解放を確認。
- 分離した検証サーバー8766で実際のplayer.htmlとTeth2素材を使用。追従量50→55→50%、左右反転と目の設定の独立、未検出時の正面合わせ案内、表示専用URLのキャラクター描画・操作UI非表示をブラウザで確認。ブラウザerrorなし。
- 実画面の保存先: `qa/camera-follow-settings-20260915.png`。撮影時はカメラ停止中。実カメラの映像は取得していない。実モデルによる姿勢推定精度、実カメラでの方向・体感、OBS本体の確認は今回未実施。
- MediaPipe設定は[公式Webガイド](https://developers.google.com/edge/mediapipe/solutions/vision/face_landmarker/web_js)、行列の配列順序は[公式MatrixData定義](https://github.com/google-ai-edge/mediapipe/blob/master/mediapipe/framework/formats/matrix_data.proto)を参照。

## 2026-09-13 素材出力・外部連携出力

- UIから単体SVG出力・SVGプレビュー・SVGパーツZIPを除去。素材出力を初期表示とし、PNG/WebM/MP4/PNG連番/シート/PNGパーツを配置。外部連携にSpriTalk/MPNG/ゲーム向けを配置。トップページのSVG出力案内も更新。
- Node全178件、Pythonのloop_export 4件・control 7件が成功。透過WebMの実エンコードでアルファ・寸法・コマ数を検証。
- 実画面のSpriTalk用出力: `data/exports/6605ed8544994a4db98aa6f48a843baf/svg-through-spritalk.zip`。18パーツ・18元PNG、設定とリグを保持し、音声設定/ローカルURL依存を除外。ZIPのCRC確認済み。
- `web/qa-integration-exports.html`でパッケージを読み戻し、1024px/supersample 2で180コマ比較。最終実行は最大差1/255・128色チャンネルのみの差（約7.55億チャンネルの比較）。`data/exports/b775c94bba5d439dbce04566c87a596d/qa-spritalk-roundtrip.zip`。SpriTalk本体での再生は未実装・未検証。
- MPNG初回で旧libvpxの遅延フレームが16コマ欠落することを発見。`lag-in-frames=0`へ変更し、完成時にデコードコマ数を検証。修正版の実画面出力は `data/exports/c4827b850d2a4207bf013d28e4a19b03/svg-through-mpng.zip`。6秒/30fps/180動画コマと180追跡コマが一致。5口差分の寸法一致と、現行SpriTalkのMpngImporterによる追跡JSON受け入れを確認。MPNGの顎・前景遮蔽の制約は説明文に記載。
- PNGパーツ実出力: `data/exports/1ca72a30d0824ae2a1249ecc0a42decc/svg-through-png-parts.zip`。18パーツ・21枚の1024px RGBA PNG、SVGファイル無しを確認。
- 実画面: `qa/material-output.png`, `qa/external-integration-output.png`。サーバーは更新版へ再起動済み。既存の保存プロジェクトは変更していない。

- 「形を整える」ではタイムラインを停止し、呼吸・揺れ・自動瞬き・音声やカメラのポーズを編集プレビューに適用しない。手動の目の閉じ具合・口の開きだけを反映。無操作で時刻00.00、描画カウンタ2のまま変化しないことを実ブラウザで確認。
- 初期工程は目。顔付近を2倍に拡大し、閉じ目を表示。手動スライダーで50%の開閉、次へで口の直接編集、口の50%で中間形状と調整ハンドルの非表示を確認。
- レイヤー欄は初期状態で折りたたみ。「3 パーツ（必要なら）」は一覧を開くだけで、実際にレイヤーを選ぶまで設定は出さない。選択後のみ従来の役割・位置等の編集欄を表示。
- 目と口の作業エリアでホイールによる拡大縮小とカーソル位置の保持を検証。拡大ボタン・ドラッグによる表示位置の移動も手動確認。「全体」で表示を戻せる。ビュー操作は素材の座標を変更しない。
- 動きの工程へ進むとタイムラインと再生が復帰。既存のAI play / seek / pose / speakが静止編集に遮られないよう、明示的な操作コマンドでは対応画面へ移る。
- `web/work-area-verify.html` のブラウザ統合検証11項目PASS。最初のカーソル保持テストは合成MouseEventの整数座標への丸めを検証側が考慮せず失敗し、入力座標を整数に揃えて再実行後PASS。イベント処理は変更していない。
- Node 56件・Python 40件PASS。操作後のブラウザerrorログなし。新規PSD変換・実マイク・OBS本体の再検証は今回の範囲外。

# スタート画面と自然な閉じ口 — 2026-09-07

- 通常の `/` 起動ではスタート画面だけを表示。最新の変換結果の自動読み込みを廃止。「新しく作る／保存済みから再開」、登録済み素材の選択→編集、編集から必要時のみ登録ダイアログを開く流れを実ブラウザで確認。フォームは1つでIDの重複なし（静的209 ID）。
- 新規の標準は母音OFF、閉じ口／開いた口の独立調整。母音ONで5母音を追加表示し、OFFへ戻すと通常開口に復帰することを確認。既存JSONの明示的な母音設定は保持。
- 元の閉じ口の線から両端を細くした形を描画。口角・カーブのハンドル、色・太さ・先細りの調整を追加。変形付きSVGなど対象外の差分は独自形状を保つ。
- 実素材の左口角をドラッグ（高さ -6.110、幅 1.321）、取り消しで高さ0・幅1へ復帰。カーブ 6.982564、開口幅0.95・高さ0.9を保存し、別画面のJSON復元で一致を確認。保存検証用コピー: `data/exports/6ba943fa196b401aa3ae5500147645ac/khaula-motion.project.json`。元のプロジェクトは上書きしていない。
- `web/mouth-verify.html` でSVGとCanvas描画を開き0 / .02 / .08 / .18 / .5 / 1の6段階で比較。位置・傾き・開口倍率・閉じ口カーブを変更した条件で、アルファ重心距離最大0.076px、アルファ総量差最大0.9%。全6ケースPASS。無音付近で圧縮した口内が残らないことをユニットテストでも確認。
- Node 54件、Python 40件PASS。操作したブラウザのerrorログなし。今回は新規PSDの再変換と実マイク・OBSの再接続は行っていない。

# 検証結果

## プレビューでの口の直接編集 — 2026-09-07

- プレビュー内に口元・顔全体・全身の拡大編集を追加。口の移動・幅・開き・傾き、閉じ口の太さ、追加する歯の上下位置・幅・高さをポインター操作できる。操作ごとの取り消し/やり直し、Escapeでドラッグ取り消し、矢印キーの位置調整を実装。
- 背景は口を除いた素材を1回描画し、口だけを独立したSVGで更新。主プレビューの毎フレーム描画を編集中は休止し、終了時に1回再構築する。口の数値入力ごとに120ms後に全素材を再構築する旧処理は廃止した。
- SVGのIDを編集用に分離。隠れている主プレビューとのID衝突で口のマスクが壊れる問題を実ブラウザで発見して修正。
- JavaScript **51件成功**。追加4件は、回転/拡縮/口の高さを含むハンドル座標と逆変換、閉じ口の幅補正、歯の局所座標と範囲制限、母音独立の取り消し/やり直しを検証。
- 実ブラウザで既存18パーツの保存素材を開き、口幅ハンドルのドラッグで「い」の幅1.18→1.572、元に戻して1.18となること、歯の高さをドラッグできること、顔全体の表示切り替えを確認。最終版の口のみのDOM更新時間は一例で15.1ms（描画完了・全体fpsの計測ではない）。
- [検証用に新規保存したJSON](data/exports/352eb80e6e8349e394c93a9ac595c779/khaula-motion.project.json)を再読込し、「い」の幅1.572・追加歯の高さ0.595が復元され、編集終了で通常プレビューに戻ることを確認。他母音と元の保存JSONは保持。
- 初期のHTTP応答欠落は今回の実行時には再現せず、稼働中サーバーのhealth本文とブラウザ表示を取得できた。保護設定には変更していないため、前回の原因候補が確定したわけではない。

## 起動確認の修正・HTTP本文欠落の切り分け — 2026-09-07

- 起動確認を100回のHTTPポーリングから、Uvicorn内部の起動完了待機と1回のHTTP本文確認へ変更。プロキシを通さず、応答が異常な場合は理由を表示して自動確認を終了する。既存ポートが使用中ならサーバーを二重起動しない。
- Python **40件成功**。追加4件は、本文検証・本文タイムアウトの判別・確認失敗時に繰り返さないこと・使用中ポートで二重起動しないこと・正常時だけブラウザを開くことを確認。
- 実際に起動中のプロセス26628へ接続し、HTTPヘッダー200/Content-Length 93が届く一方、本文は0バイトでタイムアウトすることをcurlとPythonソケットで確認。`studio.run`もその状況を表示して異常終了し、別サーバーを起動しなかった。ユーザーが起動したサーバーはそのまま保持。
- `qa/diagnose_transport.py` はその都度使い捨てのloopbackソケットで再現比較する。通常のTCP文字列27バイト・バイナリ65536バイトは全量と切断を受信。HTTPは単一/分割送信、HTTP1.0、chunked、IPv4/IPv6でも停止する。アプリ固有の描画やWebSocketを使わなくても再現した。
- ESET Internet Security 19.2.7.0が稼働していることを確認。HTTP検査との干渉が候補だが、ESET保護停止との比較はユーザー確認待ち。保護設定やネットワーク設定は変更していない。

## AI操作・OBS・素材出力 — 2026-09-07

- 最終コードの自動テスト: Python **36件成功**、JavaScript **47件成功**。APIの入力制限・接続元・編集画面の専有・処理中の競合・停止・結果通知・切断、OBS購読と再接続時のスナップショット通知、素材のフレーム計算/ページ分割/上限、マイクの無音しきい値と平滑化、素材反映の受領確認、再接続先への古い操作結果の混入防止、CLIの一覧取得・完了待機・失敗時終了コードを含む。
- 既存保存JSON `data/exports/2c4f269e67f64ece858cf66d328ae666/khaula-motion.project.json` を実ブラウザで復元。1024×1024・18パーツの編集内容を使い、接続・REST経由のpause/pose・素材出力を確認した。元のJSONは変更していない。
- 実ブラウザからREST経由で、1秒・12fps・256pxの[PNG連番ZIP](data/exports/e3c1fc39f7624bbcb1d29fbb88e4c324/khaula-frames.zip)と[スプライトシートZIP](data/exports/46a1810e433b4e88946ae9407274c4ea/khaula-spritesheet.zip)を保存。12コマ、シート1024×768、アルファ0〜255。シートの全12矩形が対応する連番PNGと画素単位で一致することをPillowで確認。再検証は `python qa/check_output_bridge.py`。
- その後、ローカルHTTPの応答本文が欠落/途中停止する現象が発生した。アプリと無関係なPython標準HTTPサーバー、最小のNode HTTPサーバーでも再現。アプリ側で応答本文の送信処理が完了していても受信側で取得できなかった。通信環境の原因特定には至っていない。
- **未確認:** OBS本体での透過表示・音声同期、実マイクでの許可/口パク、実SBV2を使った今回のREST発話。OBSブラウザ確認も上記通信問題で完了できなかった。大きな素材のHTTP取得方式への変更、受領確認、描画バッファ解放を加えた最終コードのブラウザ再検証は未完了。自動テストの成功は、これらの実機動作の保証ではない。
- 検証用に起動したローカルサーバーは終了済み。更新版は `Start-Studio.cmd` から起動する。

## 過去の検証 — 2026-09-06

実装はローカルで稼働しています。入口は http://127.0.0.1:8765 です。

## 自動テスト

- Python / API: **10件成功**。PSDのクロップ位置、レイヤー順、非表示の親、半透明、元ファイルへの非破壊出力、瞳の色の保持、書き出しの別名保存、入力/接続先制限、SBV2 APIの通信仕様と接続失敗を確認。
- Node: **9件成功**。ループ始点終点の一致、瞬き、左右独立の顔パラメータ、顔を見失った際の復帰、音量による口の開閉、マスク参照、ZIP構造を確認。
- 実ブラウザ: **9件成功**。SVGの危険な要素・外部参照の除外、ID参照整合、57%の半透明が二重適用されないこと、瞳のはみ出し防止、SMILの実際の瞬き、実PSDの通常/閉眼/口開度入力、動画用描画とSVGの一致を確認。
- MediaPipe 0.10.18: 公式WASM・Face Landmarkerモデルを実際にロードし、カメラを使わない画像入力で推論が成功。

## 実際の素材と書き出し

元PSD:
`E:\other_repo\see-through-webui\workspace\webui_output\00001-709794526_1788444876\00001-709794526.psd`

最終変換:
`data/projects/5838431d4242418baaf2af1fe2177a57/`

- 1024×1024、20パーツ、38,473パス。バランス設定、まつ毛/眉の混入除去あり、アルファしきい値12。小パーツを3倍でトレース。
- [ループSVG](examples/demo-loop.svg): 約5.96MB、ID重複なし、ラスター画像の埋め込みなし。SMILはブラウザで実動作を確認。
- [WebM](examples/demo.webm): VP9、768×768、85フレームをFFmpegで最後まで正常デコード。4秒設定に対し映像タイムスタンプは約3.8秒。約22fps相当で、初期方式の17フレーム/約4秒から改善。WebMはブラウザによる記録なので厳密なフレーム固定出力ではない。
- 音声付きWebMも合成テストWAVで作成し、VP9映像＋Opus音声の両ストリームを最後まで正常デコード。テスト音声はTTSの実モデル音声ではない。
- [PNG](examples/preview.png): 元サイズの透過PNGを生成して目視確認。
- [パーツZIP](examples/demo-parts.zip): ZIP内CRC検査成功。20パーツ＋プロジェクト・合成・ループSVGの23ファイル。
- [プロジェクトJSON](examples/demo.project.json): SVGと元画像を含むポータブル形式。ブラウザから復元し、口パク設定が維持されることを確認。
- 書き出し先が実ファイルとして存在することを確認。保存にブラウザの自動ダウンロード許可は不要。

## 実機側に残る確認

- この検証時はStyle-Bert-VITS2サーバーが停止中。既存の `server_fastapi.py` と設定ファイルを読み、`POST /voice`、`GET /models/info`、モデル/話者/スタイル/日本語テキスト/WAVの通信をモックで確認した。**利用中の実モデルを起動しての音声生成は未実施**。
- **実カメラの許可・人間の顔での追従テストは未実施**。モデルのロードと推論、および返されたblendshapeを瞬き/口へ変換する処理は検証済み。
- 開口は原画の開口または専用SVG差分を使う方式へ変更。銀髪サンプルは閉じ口を保持する。閉じ目線は簡易形状のため、専用差分へ交換できる。今回のキャラクター用に手描きの差分を作ったものではない。
- 元のAIパーツ分割にある描き足し・混入、グラデーションのトレース近似は残る。Photoshopの全レイヤー効果やLive2Dのメッシュ変形を再現したものではない。

元PSD・以前の変換スクリプト・既存成果物へ書き込みは行っていません。この作業フォルダ全体は検証時点で約176MBです。

## 追加会話の反映に伴う検証

- Node: 一回だけの跳躍と長い休止、周期の継ぎ目、髪・耳・胸の追従、パーツ倍率0、元の口内を保つ開閉、閉じ口80%幅、白目・瞳の完全非表示を追加確認。
- 実ブラウザ: 閉じ目の白目・瞳が画素として残らないこと、髪・胸・左右耳・原画の開口を含む複数時刻のSVGと動画描画の一致、SMIL閉じ口幅80%を確認。初回SMILテストはSVGを文書に追加した後の初期化を2フレーム待つように修正。
- Python: 実際のWebM映像と合成音声をFFmpegで生成し、API経由でMP4に変換。1080×1350・30fps・H.264/AAC、最後まで正常デコード、元WebM保持を確認。
- 1080pxの実素材ではMediaRecorderの初回フレーム受付が遅れ、3秒設定が約1.9秒になった。開始通知を待つだけでは2.1〜2.9秒と不安定だったため、無音ループMP4は30fpsの各コマをJPEGとして描画してFFmpegで結合する方式に変更。フレーム順・画像サイズをサーバーで検証し、テストで15コマが正確に0.50秒になることを確認。音声・カメラはライブ録画のままで、時間の揺らぎは残る。

- 保存した9.86MBのプロジェクトを実際のアプリの復元関数に渡し、一回弾み3秒、左右耳、計4つの追従パーツを復元できることを確認（web/restore-verify.html）。内蔵ブラウザの自動ファイル選択ではページクラッシュが2回発生したため、この環境でのファイル選択経由の復元は未確認。同じJSONのHTTP取得による復元は正常。

- 最終の実素材MP4: `examples/sns-single-20260906-172720/single-bounce.mp4`。1080×1350、H.264、30fps、90フレーム、3.00秒、無音。FFmpegで最後まで正常デコード。閉眼の2.1667秒を画像で確認し、白目の縁が残らないことを目視確認。
- 同フォルダの `single-bounce.svg` は24アニメーション、3秒、全チャンネルの始点と終点が一致、ID重複なし、画像埋め込みなし。ポータブルJSONと通常/弾み/閉眼の確認画像も同梱。
- この銀髪サンプルは開口と独立した胸部がないため、閉じ口と服を保持。髪・身体・左右の耳・瞬きの動きの例。口・胸・耳・髪をまとめた描画処理は専用のSVGテスト素材で検証した。

## 口パクしない問題の修正確認

- 実画面で、音声エンジンはbrowser、mouthModeはsource-closed、mouthチャンネル数0を確認。閉じ口保護の変更後に開口素材を用意していなかったことが原因。
- 開口差分を追加した同じプロジェクトを復元し、6.5秒の設定とブラウザ音声の設定を保持。実際の「生成して話す」を押し、発話中にmouth-openがscale(1 1)、閉じ口opacityが0、メーターが100%になることを確認。発話終了後にはscale(1 0)、閉じ口opacity1へ戻ることを確認。
- 開口100%の静止PNG `data/exports/0fab17710d0c4262ac4c3c8928f4f4a7/khaula-frame.png` を出力して目視確認。生成画像の口だけを利用しており、顔・髪・身体の元SVGは保持。
- Node 9件成功。サンプル限定の適用、既存開口差分の保持、別キャラクター・元絵開口への非適用を追加検証。

## 元画像＋PSDの顔と連続変形（2026-09-06）

- 元PNG `00001-709794526.png` と同名PSDの1024×1024を使用。首・服・髪の色は元PNG、白目・瞳・まつ毛・眉・口はPSD。11パーツ、20,168パスの高精細版を `data/projects/e760d04ef5a447cabaee4d5bb45bc44a` に作成。開口は既存の銀髪用差分を保持。
- Node 14件、Python 13件成功。身体にPSDの色を使わないこと、顔パーツの座標、サイズ不一致の拒否、2ファイルAPI、頭と身体の位相差、毛根の固定と毛先の遅れ、メッシュ頂点の連続性、変形行列の分解と折り返しがないことを検証。
- `web/verify-rig.html?id=e760d04ef5a447cabaee4d5bb45bc44a` の6項目成功。実SVGと動画描画の同一ポーズの平均差はRGBA 0.944/255。SMILの実変形を指定時刻の計算値と比較し、SVG単体の開口アニメーションも実ブラウザで確認。ID重複なし、全チャンネルの始点と終点が一致。
- 高精細な全体SVGを全三角形で参照する初期案では、検証専用ブラウザの描画プロセスが約10GBを使用して応答しなくなった。検証用プロセスを停止し、空間索引で必要な形のみを参照する方式に修正。パス共有で同じSVGを約52.6MBから約12.6MBへ削減。最終方式の描画とSMIL検証は正常終了。
- `data/exports/c0d656d2b96d40248f35cfbecd6bc86a/khaula-motion.mp4`: H.264 / 1080×1350 / 30fps / 120フレーム / 正確に4.00秒 / 無音。FFmpegによる最終フレームまでのデコード成功。
- 保存JSON `data/exports/b03427649a4a4fad8361b5266ce33488/khaula-motion.project.json` を実アプリで復元。11パーツ、連続変形ON、自動口パクON、首支点(518.25,517)、ブラウザ音声の設定が復元され、プレビュー描画を確認。
- TTS・カメラの入力経路は維持。今回の追加検証では実カメラや実SBV2サーバーの起動は行っていない。
- 既存のブラウザ回帰テスト9件も成功。従来テストが新しい白背景設定を片側にしか渡していなかったため、比較するSVGとCanvasへ同一設定を渡すよう修正した。
- 配布例は `examples/hybrid-20260906-183746`。MP4、同じ動き設定のSVGとポータブルJSON、閉眼と開口の確認PNGを同梱し、コピーのSHA-256一致を確認。

## PachiPakuGen参考・形を保つモード

- `E:/develop/PachiPakuGen/src/motionLabPhysics.ts` の二重バネと `src/motionLab/render.ts` の根元固定ウェーブを確認して適応。出所・変更通知・ライセンス全文を保存。PachiPakuGen本体は編集していない。
- 首の支点が座標ごとに移る計算をやめ、固定した頭・胴体の回転を首付近だけで接続。顔の局所的な輪郭変形・幅変更を止め、髪の重みから顔と服の中央を保護。毛先は硬い/柔らかいバネの周期定常応答と小さな波に変更し、最大6px/1024px幅に制限。
- Node 18件、Python 13件成功。新テストは顔・胴体内の点間距離の保存、誤った全面髪マスクでも顔・胴体を保護すること、二重バネの上限・周期性、メッシュの折り返し防止を確認。
- 実ブラウザの変形/SMIL検証6項目成功。同一ポーズのSVGとCanvasの平均RGBA差は0.937/255。口パク・瞬き・SVG単体の再生を維持。
- 修正版MP4 `data/exports/a3be9bff293f45beaf8e56df828b199e/khaula-motion.mp4` は4.00秒、120フレーム、30fps、1080×1350、H.264、無音。最終フレームまでデコード成功。
- `2c76e05dc24b4928bcc38ec26d0e2d51` の保存JSONをアプリへ復元し、stableモード、頭1.2度、毛先3px、口パクONを確認。既存変換をルートURLから開く場合も控えめな設定に移行し、元の変換ファイルが変更されないことを確認。

## 完全閉眼のまつ毛の太さ

- 完全閉眼で仮の細い線へ切り替わる問題に対応。元まつ毛PNGの暗色の主成分から太さの分布と色を抽出し、先端が細くなる塗りの閉じ目SVGを生成。自動瞬き・確認ボタン・動画・SVGで同じ差分を使う。確認ボタンのタイミングは変更していない。
- 元の変換ファイルは変更せず取得時に補完。古い保存JSONは元パーツと一致する場合だけ補完し、ユーザー指定の閉じ目や交換済み素材を保持する。再保存時に閉じ目SVGも内包。
- Node 18件、Python 16件成功。実ブラウザの描画・SMIL検証6項目成功、SVG/Canvasの平均RGBA差0.937/255。確認ボタンで閉眼した画面と、`data/exports/11cb59ec3d48426dba483cef91802ad2/hybrid-blink.png` を目視確認。
- 保存JSON `data/exports/2bb43af83f8e48a586cf764cf2cae2ea/khaula-motion.project.json` に左右の閉じ目SVGが含まれることを確認。

## 腕・前髪・後ろ髪の分離試作

- 新規変換 `data/projects/c7be0bd9d1874513a52c1505a15bc704`。PSDの可視領域を使って元画像から左右の腕・袖と前後の髪を分離。見える色は元画像、隠れる胴体と後ろ髪の下地はPSD。14パーツを描画順の6グループに分け、同じ頭・胴体の動きに各グループの揺れを加える。元画像・PSD・以前の変換は編集していない。
- 髪は波／二重バネ／停止の排他的な選択。前髪・後ろ髪の振幅、根元から毛先への重み、腕0〜2°をUIから調整・保存可能。肩支点は右(368,546)、左(689,547)。既存のパーツ支点・揺れ倍率も反映。
- Node 23件、Python 18件成功。追加項目は静止時の元画像画素の保持と隠れた下地、腕なし素材への非適用、APIの分離フラグ、描画順・設定追従、肩支点・距離保存、ゼロで停止、前後の独立性・毛先の強さ、両方式の周期性・最大設定での折り返し防止、SVGのIDとグループ別チャンネル。
- 実素材のブラウザ検証は波・二重バネそれぞれ6項目成功。SVG/Canvasの平均RGBA差は波1.065/255、バネ1.037/255。SVG内の入れ子ルートに別の時計ができる問題を修正し、単一のSVG時刻で髪・腕・口パクが同期することを確認。
- `data/exports/e55e7d66e1904308914f141bf9da6024/hybrid-loop.svg` は波、`data/exports/dd5e07d5814e499597599ee9e95d1bd7/hybrid-loop.svg` は二重バネの検証出力。波のSVGは約24MB。高精細な単体SVGの同時検証中に一時的なメモリ不足でPythonのDLLロードが失敗したが、検証画面終了後の再実行は成功。編集用プレビューはCanvas描画。
- `data/exports/4bb306cc29f146b186e2c44c84f5f066/khaula-motion.mp4` をアプリから4秒・30fpsで出力し、FFmpegで最後まで正常にデコード。
- 保存JSON `data/exports/2ea94fa4bd834eab9faea6952d39ecd5/khaula-motion.project.json` をブラウザで復元し、14パーツ、波、前髪2、後ろ髪9、毛先集中2、腕0.8°、口パクONとCanvasプレビューを確認。
- 限界: 袖と髪の境界や露出する下地に色差・小さな隙間が残る。手動マスク編集や大きな腕上げは含まない。振幅0で元の静止状態と比較可能。

## 切り口が千切れる問題の修正

- 上記の独立メッシュによる隙間を修正。現在のSVGの可視アルファと描画順から33×33の領域重みを作り、境界を滑らかに補間した一つのメッシュへ統合。前後の髪・腕の設定は保ち、切り口に別々の移動量が生じないようにした。大きな腕の分離・露出の代わりに、小さな変形の連続性を優先する。
- 保存前の設定（波、前髪2、後ろ髪9、腕0.8°）を維持。旧プロジェクトは読み込み時に境界情報を生成し、再保存時にJSONへ内包。生成前の書き出しを防止。
- Node 25件成功。新テストで全三角形の共有頂点が一致すること、単一メッシュのSVGであること、髪・腕の各設定が実際に効くこと、保存した重みの復元を検証。
- 実素材の最大設定（前髪6、後ろ髪18、腕2°、頭8°、うなずき8、追従1）で両方式・1/4/30秒を検査。三角形の行列式は最小0.5046で、折り返しなし。
- 実ブラウザ検証6項目成功。SVG/Canvasの平均RGBA差1.127/255、30フレーム58ms。`data/exports/f009fa14a3034f7bac7b88d99e753ef0/hybrid-open-turn.png` と `data/exports/20d4953f54e74d18ab64bcaa7ee27c4f/hybrid-opposite.png` を目視し、以前の袖と髪の切り口にできた隙間がなくなったことを確認。
- ブラウザが古いモジュールを保持していたため、描画モジュールURLを更新し、HTML/JSのCache-Controlをno-cacheへ変更。検証SVG `data/exports/dc2efbde9aaa421c9668fe525ecfbb24/hybrid-loop.svg` に旧グループ別メッシュのチャンネルが含まれないことも確認。
- 保存JSON `data/exports/862a40e4186c49df9f8fd7b0a4ccf263/khaula-motion.project.json` は各領域1089個の重みと既存の揺れ設定を保持。動画 `data/exports/139eed8d9335443d985d38f7478112f5/khaula-motion.mp4` は4秒・30fpsで書き出し、FFmpegで最後まで正常デコード。

## 二重バネの振幅補正と簡易5母音

- 硬い／柔らかいバネの周期応答を混合後に正規化。速度1〜4往復と柔らかさ0〜1を独立設定に追加。後ろ髪18・毛先集中2.2・4秒の実素材では、確認点(100,980)の片側変位が約5.69pxから15.05pxへ増加。毛根固定・周囲との境界補間は維持。
- Node 28件成功。期間1/4/30秒・速度1/2/4・柔らかさ0/.5/1で振幅とループ端を確認。5母音の形状差、OFF時と閉じ口の保持、かなの拗音・長音・休符・漢字の拒否を検証。
- `web/verify-vowels.html?project=5306821c6f6a4d79a93b4fd4496ffcee` の実ブラウザ検証10項目成功。5母音すべての口領域でSVG/Canvasの平均RGBA差0.679〜1.045/255。実素材の最大設定で三角形の行列式最小0.505、折り返しなし。単体SVG内の母音幅が5種類以上あり、全アニメーションのループ端が一致。
- 母音比較 `data/exports/575a1fb6078b407e979ea1960e6d23b0/five-vowels.png` を目視確認。元の口内の縦横変形なので、歯・舌の母音別差分ではない。SVG出力 `data/exports/3cfbc3e426c64790ab8cc881ef039a17/spring-vowels.svg`。
- 稼働中のローカルStyle-Bert-VITS2（5002）のモデル一覧15件を取得。モデル0 `01_khaula_tmp` で「あいうえお、あいうえお、あいうえお。」を実生成（約2.53秒）。実再生中のポーズ表示が「あ・い・う・え・お・閉」に切り替わることを100msごとに確認。音量で開閉し、かなを音声全体の長さに割り当てる推定同期。音素認識や音素時刻の精度は検証対象ではなく未実装。
- ブラウザ標準TTSは推定速度の読みトラックを使い、推定時間を超えて発話が続く場合は「あ」の開閉を継続する。5母音OFFでは読みの休符で従来の開閉を抑制しない。
- 保存JSON `data/exports/8aa6f2bec5574439a40ac78b6bd7f6d9/khaula-motion.project.json`。ユーザーの既存設定との差分は追加3項目（springCycles=1、springSoftness=.5、vowels=true）のみ。テスト用のセリフは元の挨拶へ戻し、読みも保存。
- 動画 `data/exports/2d08c9fe3562414699c038655c9a8e8d/khaula-motion.mp4` は1080×1350、H.264、30fps、120フレーム、4.00秒、無音。FFmpegで最終フレームまで正常デコード。

## 口の高さとアニメ向け輪郭の調整

- 口の高さを画像幅換算で調整するUIを追加。銀髪保存版は+6px。閉じ口と開口を同じ位置へ移動し、Canvasの素材準備時に移動を焼き込まないことで二重移動を防止。
- 新しい描き方では母音ごとのベジェ輪郭・歯・舌を共通関数から生成。口角を基準に下へ開き、5母音の間は同じパス構造で補間。元の閉じ口、元口内方式、既存のバネ設定を維持。
- Nodeの既存28件と追加2件成功。無音の不可視化、OFF時の母音抑制、丸口の歯の非表示、パス構造の共通性、ループ端、位置の解像度換算を確認。
- 実ブラウザ10項目成功。5母音のSVG/Canvas口領域差は0.509〜0.516/255。比較画像 `data/exports/482b0c89d3264d1e9c49d43a8847c526/five-vowels.png` を目視確認。SVG `data/exports/55d8dd34a44c4ed9bfb8f3b1692b32db/spring-vowels.svg` は輪郭アニメーションとループ端一致を確認。
- 保存JSON `data/exports/1ab75f9f69a64b5a97cdb332048409fe/khaula-motion.project.json`。4秒MP4 `data/exports/44d33a2b27604eee9ece9b197d78c7dd/khaula-motion.mp4` は正常デコード。同フォルダの `closed-mouth.png` で閉じ口と周辺の肌を目視確認。

## 入力を開眼・開口の原画に統一

- 新規インポートの前提を「両目と口が開いた1枚絵」に変更。UIの目視確認とAPIのopen_features宣言が必須。自動的に開閉を画像判定する機能ではない。閉じ口の内蔵サンプルは新規変換対象から外した。
- 元画像＋PSD変換では、口の領域だけPSDのアルファを使い、線・歯・舌・口内のRGBは元画像から取得。口をsource-openとして保存し、描き足し差分の自動挿入を撤去。通常PSDインポートも宣言済みの口をsource-openに設定。一枚絵単体は従来通りSVG化のみで、顔パーツの自動分離は追加していない。
- Python20件成功。元絵とPSDで異なる口色のテスト素材を作り、抽出口が元絵の画素と一致し、生成差分が入らないことを確認。宣言なしの2系統のAPIと旧サンプルが422で停止すること、宣言済みが変換処理へ渡ることも検証。
- 実際に開眼・開口した新しいイラストはまだ受領していないため、実キャラクターでの新規変換は未実施。作業用JSON `data/exports/ea697604cb894c1e84dce9602247a699/khaula-motion.project.json` は旧銀髪の描き足した口を外し、原画の閉じ口で素材差し替え待ち。以前のJSONや元画像は保持。

## 読み込みUIの統合

- 1ファイルのドロップ欄、旧サンプルボタン、折りたたまれていた別のインポート経路を撤去。元画像と対応PSDを順に選ぶ一つのフォームに統合し、変換設定はその中に配置。
- JSも2ファイルのインポート専用に整理。両ファイルと開眼・開口確認を必須化し、拡張子と各100MB上限を送信前に確認。変換中はフォーム全体を無効化し、終了・失敗後に再操作可能に戻す。
- Node構文チェック成功。既存ブラウザタブを更新し、入力が2つ・実行ボタンが1つ、未選択の送信がHTMLの必須検証で止まること、14パーツの既存プロジェクトが復元されることを実画面で確認。新たな素材の変換はこのUI整理では実行していない。

## MP4・WebMの輪郭と階調

- ユーザーの直近MP4の圧縮前JPEGにも、SVG化由来の輪郭の荒さと色の段差を確認。WebMは768px制限も残っていた。
- ハイブリッド素材の未編集staticパーツについて、元の抽出PNGを同じメッシュで描画するモードを追加。SVGのID接頭辞を正規化したSHA-256を照合し、内容が一致するパーツだけに適用。SVG差し替え後は画素参照を無効化。今回の実素材は前後の髪、胴体・顔の下地、左右の腕の5パーツが一致。顔パーツのSVGと瞬き・口パクの処理は継続。
- 両動画の長辺1080px化と最大2倍描画後の縮小を追加。MP4中間画像をPNGにしてJPEG段階の損失を撤去、CRF17へ変更。旧JPEGフレームZIPも読み込み可能。WebMは12Mbps、30fpsを目標とするライブ録画。
- Python20件成功（PNGフレームZIPのMP4変換を追加確認）。Nodeの追加テストでID接頭辞が違う保存SVGの照合、編集後の不一致、SVG描画の指定を確認。
- 実ブラウザ `verify-video-quality.html` は5項目成功。連続階調の画素保持、2倍描画からの指定サイズへの縮小、編集済みSVGの優先、実素材5パーツの一致、1080px出力を確認。`data/exports/d22d48b42fde49fb9f327809c50efab4/video-quality-comparison.png` を目視し、髪の輪郭と肌の段差の改善を確認。
- 設定JSON `data/exports/f32d2ec853154905ad6e1761566df705/khaula-motion.project.json` は現在の動き設定とPNG・照合情報を保存。MP4 `data/exports/61bf5033c5b14d5cb9975d9001230f25/khaula-motion.mp4` は1080×1350、30fps、120フレーム、4.00秒、全フレーム正常デコード。WebM `data/exports/cd9e62e2e7894b60983b2e39d732c70e/khaula-motion.webm` も実アプリから書き出して映像デコード成功。WebMはライブ録画のため、今回の4秒設定では開始待ちを含む約5.61秒・121フレームになり、フレーム間隔の揺らぎが残る。FFmpegのnull出力に重複DTSの警告があり、正確な尺と固定30fpsは無音MP4を使用。
- 単体SVGのベクター品質はこの変更では改善していない。原画描画を持ち運ぶJSONの容量とPNG中間画像の容量は増える。
# 閉じ目の左右の傾き（2026-09-06）

- `studio/eyelids.py`: 元まつ毛の左右端付近の高さを個別に測り、閉じ目の傾きと緩いカーブに反映。塗り形状の太さと色を保持。
- 従来の自動生成形状に一致する場合だけ読み込み時に更新。手動差分と変換元ファイルは保持。
- Python 22 tests pass（左右反転時の傾き、範囲、旧形状の移行、手動差分保持を含む）。JavaScript構文確認済み。
- `web/verify-eyelid-tilt.html` で実素材2眼の移行と共通Canvas描画を確認。UI保存後も両眼更新、設定・他パーツの一致を確認。
- 比較画像: `data/exports/7ac471b92310429298f86e06ce56540e/eyelid-tilt-comparison.png`
- 保存プロジェクト: `data/exports/e2fe94a402db4b7da01b0f65595f59e9/khaula-motion.project.json`
# まつ毛の毛先・前髪の残像・上下向きと髪範囲（2026-09-06）

- 閉じ目に下向きの尖った塗り形状を追加。前髪に隠れすぎない位置に配置し、元パーツの高さを越える分は差分SVGの描画枠を拡張。
- 元絵のまつ毛が前髪に混入した箇所は、PSD側の髪色が明るい場合だけ局所補正。今回296ピクセル、胴体下地と前髪へ反映。前髪の全面置換はしていない。PSD自体にも残像がある場合は自動修復できない。
- 顔の上下向き（固定値＋ループの揺れ）、前後髪ごとの曲げ始めY・毛先Y・中心X・半幅を追加。Still2Rigの深度差・根元/毛先指定の考え方を参考に、このアプリの共有変形場で実装。髪を独立した房へ切断する処理ではない。
- Python 24、Node 33 tests pass。実素材で上下±1・16時点のメッシュ最小行列式0.663、狭い髪範囲指定でも0.765。共有Canvasから閉眼・上下向きのPNGを書き出して目視確認。
- UIで後ろ髪の範囲指定200/950/800/200を入力し、保存・再読み込みで復元を確認。試験値は解除して納品。
- 最終プロジェクト: `data/exports/85024c58e0344a19b9167bb6eacce001/khaula-motion.project.json`
- 比較: `data/exports/c61973f88080424e959bc0f665657cc8/hair-lash-comparison.png`
# 開眼時の髪上のまつ毛を保持・前髪の振幅（2026-09-06）

- 局所修復で取り除いた296pxを元画像から独立した左右の重ね描きとして保持。開眼時は元の色を再現し、各眼の瞬き0〜0.6で消す。共通SVGチャネルとCanvasに実装し、保存・復元に対応。新規ハイブリッド変換でも同じ素材を生成する。
- 前髪の自動毛先位置を首から目付近へ変更。今回の保存済み範囲指定もY=517から337へ調整。揺れは8、UI上限12。根元90、範囲の中心・半幅、パーツ倍率など他の指定は維持。
- 前髪の代表点(480,300)で最大変位0.74→4.10px、(530,300)で0.62→3.49px。身体・後ろ髪を除いた前髪の寄与を計測。
- 実素材24時点、前髪上限12、波・ばねの両方式でメッシュ反転なし（最小行列式0.661）。Python25、Node35 tests pass。UIに16パーツ・前髪8・毛先337を確認し保存。
- 最終保存: `data/exports/22744c9e3845402ea1bb694fee9a86f9/khaula-motion.project.json`
- 開眼／閉眼比較: `data/exports/cc211be051814648b49fb2e6ad3e7d1a/lash-overlap-open-closed.png`
# ユーザーによる閉じ目編集・房ごとの髪制御（2026-09-07）

- 「閉じ目の形を調整」で左右別の横位置・高さ・傾き・横幅・太さ・カーブ・トゲを編集。元の閉じ目SVGと調整値を別々に保持し、各眼のリセットが可能。カーブ・トゲは生成済みの輪郭形状に対応し、任意の追加SVGでは位置・傾き・幅・太さに限定する。
- 表情固定（両眼／片眼）は確認専用。固定時に再生を止め、解除時に以前の再生状態を復元。通常ループ出力の瞬きは維持される。
- 前髪・後ろ髪に最大8房。初期3房。根元XY・毛先XY・半幅・倍率・遅れを個別に保存。Still2Rigの房別の重み付けの考え方を参考に、既存の共有変形場へ実装。房同士の重みをなだらかに混ぜ、画像の切断はしない。
- 画面に根元●・毛先◇を表示し、各点にポインターで位置を変える操作を実装。数値入力・房追加／削除・表示切替を用意。ガイドは出力画像へ含めない。
- Node38、Python25 tests pass。UIで閉じ目の高さ3・カーブ4・太さ0.7の保存とリセット、房の毛先440・遅れ0.35の保存を確認。試験値は戻して最終保存。
- 実素材の開眼フレームは、閉じ目の調整有無で全ピクセル一致。3房の波・ばね、32時点でメッシュ反転なし（最小行列式0.864）。閉眼比較PNGを目視確認。
- 最終保存: `data/exports/05d92c97fab745e4a0e12ba8967df21e/khaula-motion.project.json`
- 編集比較: `data/exports/544e440f6c7f47faabae6078a03f4ee8/editable-eyelids.png`
# 音声ボタンが無反応に見える問題（2026-09-07）

- 開口素材の有無を音声生成の必須条件にしていたチェックを撤廃。開口素材がなくても音声は生成・再生でき、音声のみであることを表示。素材の入力要件・口パクの準備判定は維持。
- 生成ボタン直下に進捗・完了・エラーを表示し、準備中・生成中・読み上げ中はボタンの表示を更新。停止時は押せる状態へ戻す。古い要求の応答で新しい再生状態を変更しないよう世代番号を確認。
- ブラウザ音声の開始通知が8秒間来ない場合は、状態を戻してエラーを表示。音声ファイルの再生完了・再生エラーも同じ場所へ表示。
- 実際のユーザー設定（ブラウザ音声・「こんにちは。」）で開始→完了、ボタン復帰を確認。Style-Bert-VITS2でも0.928798秒のWAV生成・再生完了とWAV保存可能状態を確認。空白のセリフでボタン直下のエラー表示を確認。JavaScript構文チェック済み。
- エンジン・セリフ・読みを元の設定に戻して保存: `data/exports/0f740112eba141b6bf2974c34d63e2bf/khaula-motion.project.json`

## 2026-09-07 顔の上下向きと髪上の目元の追従

- 髪上に残るまつ毛オーバーレイが static 扱いで顔の視差移動から外れていたため、対応する lash-l/r の変換へ追従。小さなクロップの中心と本体中心の差を補正し、soft モードの横幅変形でも接合位置を一致させた。Canvas と SVG の双方に適用。
- 元画像＋PSD の読み込み時、前髪内に焼き付いた眉の暗色も PSD の前髪色から下地を復元し、元絵の線を brow-l/r に追従する前景として保持。眉は瞬きでは消えない。同一画素を眉とまつ毛の両方へ割り当てない。共有メッシュの所有領域からは両オーバーレイを除外。
- 現在の保存データ 4fcb6a5e467b40ea90f526daeeb1c4fd を別コピーで移行。修復範囲179画素、既存 core/front のみ修復、眉オーバーレイ2つを追加して18パーツ。編集済み SVG と元画素の不一致は移行ツールで拒否。
- ユーザー設定、音声設定、閉じ目調整、髪の房／範囲／強さは保存前後の JSON で一致。UI 保存・再読込で faceOverlay と blinkOverlay が保持されることを確認。
- Node 39件／Python 25件成功。追加テストは上下±1・左右±1・stable/soft の本体とクロップの座標一致、SVG出力チャンネル、眉の非点滅、所有元欠損時を確認。
- 実Canvas描画で上下 -1/0/+1 × 開眼/閉眼を目視確認。確認ポーズのメッシュ最小行列式0.893、反転なし。正面・開眼の移行前後画像は全RGBA成分の平均差0.00005/255（最大44の少数境界画素）。
- QAページ: web/verify-face-overlays.html。比較画像: data/exports/cd4fe6298f4c4c2685db94c9cc2b168a/face-overlays-pitch.png。
- アプリで保存した完成コピー: data/exports/4c455ca5028d4c829a27541c3d82fbad/khaula-motion.project.json。
- 元絵の限られた重なり部分を追従させる近似であり、大きく顔を向けた際の隠れた描画を新しく生成する処理ではない。閉じ目自体の画風調整は既存の左右別編集UIを引き続き使用できる。

## 2026-09-07 開口素材の位置ずれと閉じ口の消失

- 対象は新しい開口素材 95c8712db1284360af48e061ffdc878b。PSD mouth の画面端に alpha 12〜30 の孤立画素があり、クロップが1024×1024となっていた。口の支点が実際の(530,419.5)から(512,512)へずれ、母音・開閉時に顎下へ移動していた。
- clean_alpha の mouth 処理で実体のあるアルファ成分につながらない薄い孤立画素を除外。つながったアンチエイリアスと半透明の口は保持。修復後の口は x493/y396/74×47。首・顔の自動支点も正しい口中心から再計算。
- 元の口輪郭の色・上端カーブから閉じ口の線を生成。開度0〜18%で元絵の開口と連続的に切り替える。閉じ線は高さを潰さず、閉じ口幅のみ適用。既存の手動閉じ差分は保持。
- Canvas のテクスチャ準備時は閉じ口幅を1に固定し、描画時だけ幅を適用。SVGも同じ透過率・幅・位置を使用。
- 同じ入力2ファイルから別プロジェクト539541d7150b4864aa76fac8377cc5e5へ再変換。元の変換とアップロードは保持。UI保存コピー: data/exports/c3d0bbc0bc0f4aafa04badbb6629574b/khaula-motion.project.json。
- Python28件、Node40件成功。PSD端の薄いゴミを含む実変換のクロップ位置、半透明・縁の保持、閉じ線の色と太さ、切り替えの連続性を検証。
- web/verify-mouth-crop.html: 実ブラウザで0/50/100%を目視。0/9/50/100%のCanvasとSVGの口境界を2px以内で照合。閉じ口も可視で中心が維持される。比較画像: data/exports/16fff8ea5ce546d897232fd6f9b57e25/mouth-opening.png。

## 2026-09-07 ユーザーによる口の形の編集

- 「動き」内に「口の形を調整（閉じ口・あいうえお）」を追加。閉じ口は角度・幅・位置・線の太さ・カーブ、各母音は幅・開き・角度・位置を独立して編集できる。
- 母音別に追加の歯をオン／オフし、横幅・高さ・上下位置・色を調整可能。元素材の口の色と輪郭から取得した内側マスクで歯を制限する。現在の「い」は歯オン、開き0.4、歯の高さ0.35・上下位置0.3で保存。他の母音と閉じ口は元の設定。
- mouthTuning の値は正規化し、通常のスライダー変更でも保持。形ごとのリセットは他の母音に影響しない。元SVG・閉じ口差分は変更しない。音声／カメラ入力中は編集プレビューより口パク入力を優先。
- CanvasとSVGは同じ母音の重み・変換・歯のマスクを利用。SVGのアニメーションはtranslate/rotate/scaleで実装し、未対応のSMIL matrix型を使わない。
- Python29件、Node42件成功。調整値の保存形式、各母音の独立性・補間、閉じ口復元、支点位置、マスク内に歯が収まることを検証。
- 実UIで「い」の歯と開きを変更、閉じ口の角度-12／幅1.2／太さ1.5を保存・再読込して保持を確認。その後、閉じ口のみリセットし「い」の変更が残ることを確認。
- web/verify-mouth-editor.html: 6形状を目視。閉じ口・開度9%・い・う・母音混合のCanvas/SVG境界差2px以内、口周辺のアルファを考慮したRGB平均差0.025〜1.927/255。単体ループSVGも生成済み。
- 比較画像: data/exports/8ae8b1ca306142cfb95e8069a3e547e6/mouth-shape-controls.png。
- 完成プロジェクト: data/exports/2c4f269e67f64ece858cf66d328ae666/khaula-motion.project.json。
# SVG-Through ワークスペース再構成 — 2026-09-07

- 実ブラウザで既存18パーツの保存JSONを読み込み、中央プレビュー、5つの作業メニュー、口・目・パーツ、モーション、音声・マイク・カメラ、配信、出力の入口を確認。
- 口の「い」の横幅をプレビュー上でドラッグし、取り消しが有効になること、取り消し後に編集を終了できることを確認。レイヤー選択でパーツ設定が表示されることを確認。
- カメラ設定から別作業へ移動し、「声と表情」に戻ったとき、音声の内容と選択状態が一致することを確認。
- 素材読み込み・復元はモーダルへ移動。元の静的コントロール172個を維持し、再構成後197個のIDに重複がないことを確認。
- 新しい出力画面から1秒・12fps・256pxのPNG連番を実出力。`data/exports/890d7169706544568761990d397b45f0/khaula-frames.zip`のZIP整合性、12枚すべてRGBA・256×256・透過ありを検査。
- Python unittest 40件、Node 51件成功。変更したアプリ・ナビゲーション・出力モジュールの構文確認成功。操作後のブラウザerrorログは空。
- 今回は実マイク・カメラを起動しておらず、OBS本体との接続も再検証していない。直接編集の対応範囲は口と髪の房のガイド。初回読込と実際の素材変更時には描画準備が残る。


## 2026-09-08 目・口の直接変形と重ね表示

- 閉じ目に幅・カーブ・位置・傾き・太さの画面ハンドルを追加。左右を直接選択でき、数値は折りたたみ。1ドラッグ単位のundo/redoとEscapeによる取消。
- 目78%、口11%の重ね表示を編集時の初期値に設定。口の閉じ形ハンドルを中間表示でも残し、実際の口幅の補間と座標を一致させた。確認後に重ね表示へ戻すボタンを追加。
- 閉じ目だけテクスチャを差し替え、古いテクスチャを解放。準備中の変更を待ち合わせ、rigのない素材でもCanvasの編集面を用意する。
- Node 60件 / Python 40件成功。web/work-area-verify.html は初期値・ハンドル・静止・拡大縮小・任意のパーツ編集・アニメ再開の14項目成功。
- web/eye-render-verify.html は部分更新と新規レンダラーの比較で、目78%/100%・通常/2倍描画のすべてのピクセル差0。更新中のdisposeも成功（6項目）。
- 実素材2c4f269e67f64ece858cf66d328ae666で目中央と口中央を実マウス操作。双方のカーブが追従しundoで復元。口11%でハンドル可視、数値欄は初期状態で閉じる。ブラウザerrorログなし。保存済みの素材・プロジェクトは変更していない。


## 2026-09-08 任意の追加PSDから目・口の差分を取り込む

- 初回読み込みに閉じ目・閉じ口・5母音の任意PSD欄を追加。multipart APIから変換ジョブへ渡し、指定した表示レイヤーだけを抽出。キャンバス不一致・対象レイヤー欠落・拡張子・サイズを検証。差分はSVGの位置を保持してベースに登録する。
- 閉じ目・閉じ口はカスタム差分として保持。母音ごとの `mouthVariants` を保存・検証・復元し、SVG / Canvas / アニメーションSVG / パーツZIPで共通使用。追加母音の初期変形は1倍、未追加母音はベースへ戻る。
- 実素材は Desktop/一時退避/02_SpriTalk・See-Through関連/See-Through用素材。open_a.png + open_a.psd に close_eye.psd、open_eye.psd の閉じ口、5母音を追加して実変換。元PSDは変更していない。
- 直接変換 4cb4337da2464b99902c064c7c601aff、UIから保存した可搬JSON c26d17f5b56a40818e2adaf3626b51d9/khaula-motion.project.json（14,478,100 bytes）。保存JSONを再読込し、PSDの「い」「う」の異なる口形・閉じ口を実画面で確認。
- サーバー更新後の実HTTP multipartでも全7差分のアップロード・変換・取得成功。変換ID 9da2558f65b947839505a85c8f4968f1。health.faceDonorImport で対応可否を確認し、旧サーバーが差分を無視することを防ぐ。
- Python 44件 / Node 62件成功。web/face-donor-verify.html は16項目成功。5母音・混合・完全閉口ではSVGとCanvasの比較差0。中間開度11%は縮小ラスタの縁の差5.34%（RGBAをalphaで重み付け、許容8%）。母音混合＋11%開口は4.00%。閉口の重ね順と母音の合成後の透過もSVGとCanvasで統一。
- 同画面でSVGの登録座標・overflow保持・JSON往復・アニメーションSVGチャンネルを確認。実素材のブラウザerrorログなし。OBS本体での表示、実音声による母音認識は今回の検証対象外。

- 最終回帰: 従来の mouth-verify 6ケース成功（中心差最大0.076px、alpha量差最大0.9%）。work-area-verify の14項目も成功。

## 2026-09-08 作り方を選ぶ導入と差分の確認フロー

- 導入を「基本素材から作る／用意した差分を使う」に整理。基本素材はどちらも開き目・開き口の画像＋PSD。差分は目・口それぞれで採用／下書きを選べ、閉じ目のPSDを閉じ口にも共用できる。母音は任意の折りたたみ欄。
- 非選択の差分入力は無効化し、送信時も選択状態から対象を組み立てる。モードを戻した後の古いファイルを誤送信しない。PSD採用を選んだ部位だけファイルを必須にする。
- PSDの閉じ形がある部位は開閉確認から開始。手動スライダー・重ね表示・拡大縮小を保ち、編集欄とハンドルは「位置・大きさを調整する」で表示。目と口で独立判定し、差分のない部位は従来の直接編集。母音の詳細は読み込み時に折りたたむ。
- Node 67件成功。web/import-flow-verify.html の15項目（選択、PSD共用、混在、送信対象、保存済み差分の案内・編集切替）と web/work-area-verify.html の14項目（従来の直接編集・静止・ズーム・モーションへの遷移）成功。実素材を使った目・口の確認画面、導入の基本／差分表示をブラウザで確認。
- 既存の変換API・SVG描画方式・素材ファイルは変更なし。今回は新規のPSD再変換やOBS本体の再検証は行っていない。

## 2026-09-08 SVG-Through Motionへの表示名変更・公開前レビュー

- ヘッダー、ページタイトル、OBS、SVGタイトル、起動表示、APIアプリ名、READMEをSVG-Through Motionに変更。保存ファイルの内部識別名・旧保存URLは互換性維持のため据え置き。
- 500px以下のヘッダーを折り返し、新しい名前で上部ボタンが切れる問題を修正。390pxの埋め込み画面で有効幅375px／文書幅375px、保存・書き出しボタンの表示を確認。1280pxの編集画面も確認。
- Node 67件成功。PythonでAPIアプリ名を確認。初回・再開・目・口・モーション・OBS設定・出力の実画面をレビューし、編集タブのブラウザerrorログなし。
- 詳細は docs/public-release-ui-review.md。保存と再開、PSD準備、重ね表示の説明、用途別の出口を優先課題として記録。ユーザーテスト、クリーン環境起動、実OBS接続は今回未実施。

## 2026-09-08 開いた表情からの案内と共通の変形枠

- 最初は開き目・開き口を表示。「閉じ目／閉じ口を確認する」で完全閉鎖へ進む。開閉スライダーと任意の重ね表示は確認後に表示し、PSD差分の部位は確認を基本とする。
- 閉じ目・口に共通の8ハンドルの変形枠を追加。枠内移動、辺で幅・高さ、角で縦横比を保った拡縮、上の丸で回転。Shiftで自由な比率、Altで中心から拡縮。通常は反対側のハンドル位置を固定し、負の倍率への反転を防ぐ。口角・カーブは別ツール。PSD差分では未対応のカーブ操作を無効化する。
- 独立したheightを目と閉じ口の保存設定・SVG描画へ追加。既存データの既定値は1。数値欄・undoにも反映。目の部分更新と口の背景キャッシュは維持。矢印で微移動、ドラッグ中Escapeで取消。
- Node71件成功。work-area-verify17項目、import-flow-verify15項目成功。heightを変えた目の部分更新／新規描画比較6項目はピクセル差0、口のSVG／Canvas比較6項目も成功（中心差最大0.044px、塗り量差最大1.1%）。
- 実マウスで目・口の角ハンドルをドラッグし、縦横比を保つ倍率の変化とundoを確認。カーブツールの切替を実画面で確認し、ブラウザerrorログなし。既存のユーザー素材・保存プロジェクトは上書きしていない。

## 2026-09-08 読み上げエンジンの選択

- SBV2・VOICEVOX・AivisSpeech・Irodori-TTS（Aratako版OpenAI互換）・ブラウザ標準を共通の声選択UIで扱う。エンジン別の接続先・声をプロジェクトに保存し、APIキーは保存しない。外部AIのspeakは選択中のエンジンを使用する。
- VOICEVOX/AivisSpeechのstyle ID、audio_query全体の維持、Irodoriのモデル・声一覧とspeech JSON、SBV2互換性、接続制限・不正レスポンスをMockTransportで検証。ブラウザ音声の終了待ち・中断・エラーは模擬SpeechSynthesisで検証。
- Python50件、Node74件成功。tts-ui-verify.htmlの10項目（エンジン選択、声一覧、切替、設定保存・復元、認証情報の除外、標準音声の表示）成功。実編集画面で声選択欄を確認。バックエンドを再起動し、healthの対応エンジン一覧を確認。
- 実TTSサーバーは起動していないため、各実エンジンでの音声生成・音質・実時間同期は未確認。ブラウザ標準の実音声、OBS本体も今回未検証。プロトコルの模擬試験を実エンジン試験として扱わない。

## 2026-09-08 カメラ開始時の停止対策

- ユーザー画面は「モデルを読み込んでいます」のまま開始・停止とも無効。MediaPipe初期化とdetectForVideoをメインスレッドからclassic Workerへ移し、描画・入力処理と分離した。従来と同じ0.10.18のライブラリ・モデルを使用。
- 準備段階をカメラ欄に表示し、開始中もキャンセルできる。モデル90秒・映像再生15秒・権限待ち30秒・解析10秒の期限を設けた。キャンセル後に届いたストリーム・画像は解放し、古い解析結果は採用しない。権限拒否・未接続・使用中を個別案内する。
- Node80件成功。実ブラウザのface-tracker-verifyで実モデル初期化・空画像解析に成功。camera-ui-verifyの6項目（準備中取消、再試行、実モデル＋canvas模擬ストリーム起動、解析値反映、停止解放、権限拒否の案内）成功。実カメラ映像や顔そのものは今回取得していない。
- 元のユーザータブは保存操作の完了応答がなく、未保存内容を保護するため再読込していない。修正版は別タブの検証ページで確認した。
- 設計根拠: https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker/web_js （同期のdetectForVideoはUIをブロックするためWorkerを利用する案内）。

## 2026-09-08 白い袖が透過する問題

- 9da2558f65b947839505a85c8f4968f1の元画像(740,850)はalpha255、clean-plateとp003はalpha0だった。画像下端につながる白い袖を、白背景のflood fillが消していた。変形・不透明度設定の問題ではない。
- 背景除去で可視PSDパーツのalpha128以上を保護。背景名のレイヤー、全面不透明レイヤー、非表示レイヤー、薄い残留alphaは保護根拠にしない。元画像に透過がある場合は、そのalphaを優先して白背景除去を行わない。変換結果にpsd-protected-v1を記録。
- Python54件成功。白い袖が下端につながる場合、背景/非表示レイヤー除外、薄いPSD残留、既存半透明の維持の4件を追加。
- ユーザーの現在の調整をb67fb7b9f9994fd197c63f7a213c934cに保存後、署名・座標が一致する未編集の左袖p003だけ10925画素を元画像から復元。別の保存0d9a9ee25458485390823861b91094c1を作成。その他のパーツ、目口差分、モーション、音声設定はJSON比較で同一。元のPSD・画像・保存を上書きしていない。
- sleeve-alpha-verifyで修復前alpha0→修復後alpha255を確認。元画像描画・SVG描画の両方が成功し、比較画面で透過穴の解消を目視確認。変換サーバーを再起動して新規読込にも修正を反映。

## 2026-09-08 5母音を通常UIから外し、開閉に集中

- 母音追加読込・母音設定・読み仮名を通常UIから非表示。母音ファイルはUI送信対象から除外。旧保存もlipSyncMode=open-close/vowels=falseで開き、保存・プレビュー・OBS・素材出力に共通設定を渡す。母音SVG・調整値のデータは保持。
- 録音/TTS/マイクの音量追従を共通の時間ベース処理にした。開始/継続の閾値を分け、90msの短い途切れを保持。開き35ms/閉じ85msの時定数で追従し、長い無音で0に収束。明示停止時はリセット。ブラウザ標準は推定動作のまま追従を滑らかにする。
- 開閉差分の重ね表示範囲を開き3.5～8.5%に縮小。手動調整用の重ね表示は6%に変更。開閉時には不要な5母音の描画素材を作らず、保存データのみ残す。
- Node84件成功（短い途切れ/無音復帰/ノイズ/停止/フレームレート依存/マイク閾値を検証）。open-close-verifyの11項目成功。旧母音ONの無効化、編集形2種、読み非表示、保存設定、5つのSVGと母音調整値の保持を確認。実TTS/実マイク音声の聴感評価は未実施。

## 2026-09-08 閉じ形の確認から画面編集へ案内

- 目は元の位置と目頭・目尻、口は位置・横幅・傾きを確認する案内を表示。「画面で位置・大きさを直す」ボタンから重ね表示と変形枠を同時に出す。調整完了で閉じ形の確認に戻り、「この目／口でOK」で次へ進む。
- 開閉スライダーと表示切替を「開閉の途中も確認する」に集約。数値欄は微調整用として維持。PSD差分では使えないカーブツールを非表示。shape-guideに残っていた口の重ね表示11%を共有定数6%に統一。
- Node84件成功。import-flow-verifyの22項目成功（差分PSDと生成閉じ形、確認ポイント、補助欄、ボタンから編集枠表示、完了時の閉じ形復帰）。実画面で閉じ口の確認・編集状態を目視確認。初見ユーザーによる操作テストは未実施。
- 作業中プロジェクトを1e6eb86ae8564ec0b75c3060211464bcに保存し、その保存を更新版で開いた。

## 2026-09-08 瞬きの手動確認を最初の操作にする

- 「閉じ目を確認する」ボタンと折りたたみ内のスライダーを廃止。作業エリア直下に横幅いっぱいの手動スライダーと26pxのつまみを常設し、開く→閉じる→開くを最初に確認する。口の工程では口の開閉スライダーへ切り替える。
- 右側は「位置がずれる」「長さ・大きさが合わない」「傾きが合わない」から補正箇所を選ぶ構成にした。選択時に重ね表示と編集ハンドルを出し、移動・拡縮・回転のどのハンドルを触るか個別に案内。生成した閉じ形ではカーブ補正も選べる。補正中もスライダーから確認へ戻れる。
- PSD差分・自動下書きとも確認から開始し、補正は任意。Node84件、import-flow-verify26項目、work-area-verify17項目が成功。実画面のスライダーをキーボードで両端まで操作し、開閉描画と大きさを目視確認。
- 更新前の作業内容（モーション調整を含む）を9d97cbc81caf4297bed3501b5c13ad2fに保存し、その保存を更新版で開いた。

## 2026-09-08 See-Through前髪の比較試作

- 髪上の睫毛補助レイヤーは位置固定のまま閉じ率60%までフェードするため、36%でも不透明度35.2%で残る。早期フェード案を試したが、ユーザーの素材差し替え案を受けてコード・テストの変更は元に戻した。
- open_a.psdのfront hairをRGBA PNGとしてa2ce09c2835e4184b01d744ff3e32c07/see-through-front.pngに抽出。web/front-source-compare.htmlで、作業内容を保存した8ebb0a37f5534fa5b9e0b76d10df96edをもとに比較した。
- 右側だけ前髪をPSDのPNGへ置換し、元絵用のblinkOverlay/faceOverlayを非表示。開き率0/36/100%の静止Canvas比較を生成。36%で元の位置に残っていた睫毛の影が右側で解消することを目視確認。髪の色・輪郭はPSDの描写となる。
- 比較はrigEnabled=falseの目元表示。髪の揺れ、保存互換、素材出力の正式な差し替え導線は未実装。ユーザープロジェクトの素材は変更していない。

## 2026-09-08 閉じ目の上のぼかしを切り分け

- donor-eyes_closed-lash-l.pngに閉じ目上の細い線が含まれており、この線は残像ではなく差分素材の描写。一方、目を非表示にした下地にも灰色のぼかしが残っていた。
- open_a.psdのfaceレイヤーを抽出し、目パーツの周囲と既存face-removalの共通領域9502pxだけ顔素材で補修した比較PNGを5e5b7684e81e4f8696a20717ad92e019に作成。alphaと領域外は保持、境界4pxを混合した。
- web/eye-background-compare.htmlで現在/補修、閉じ目あり/下地のみの4パネルを描画。灰色のぼかしは減少したが、元絵との肌色差による境界が見えるため、現段階は比較試作。正式な変換処理・ユーザーの保存プロジェクトは変更していない。

## 2026-09-08 動く睫毛を髪の重なり部分だけ表示

- 任意設定eyeThroughHair（既定false）、eyeThroughHairStrength（既定0.35）を追加。前景の髪のalphaマスクで、通常描画と同じ開閉中の睫毛を前面へ合成する。頭・前髪の変形も各パスに反映する。ON中のみ旧blinkOverlayを非表示、元の素材データは保持。眉補助・顔下地の比較試作は変更しない。
- Canvasは通常・前髪マスク・睫毛の3パスをキャッシュ。強度のドラッグでは再ラスタライズせず合成alphaだけ更新する。閉じ目の調整は通常と睫毛パス両方へ反映。SVGも同じマスクと複製された動的チャンネルを使い、ループSVGにも含める。
- Node87件、Python55件成功。eye-through-verifyの15項目成功：重なり以外不変、開/途中/閉のCanvas・SVG画素一致、固定残像なし、半透明髪、強度変更、閉じ目編集、頭・髪の変形中の一致、アニメSVG、旧保存OFF、ON/強度の保存復元。OBS本体は未起動、共通Canvas経路と既存接続テストで確認。
- ユーザーの調整をaf96c499b5d240a8b9d5fe044a7acadeへ退避後、更新版で35%をONにし、開き目・閉じ目を目視確認。API設定の追加を反映するためサーバーを再起動（PID29756）。
- 35%をONにした保存は0a5d2ed86b624d4189e5818be42aad4b。ユーザー画面もこの保存を参照するURLへ切り替えた。

## 2026-09-09 前髪素材の差し替えを実際の保存と変換処理に適用

- 前回は重ね表示の機能のみ適用し、前髪素材の差し替えは比較試作に留まっていた。元絵由来の前髪に残る睫毛を消すため、新規ハイブリッド変換はPSDの前髪RGBAを使い、旧blinkOverlay/faceOverlayを生成しない。分離OFFでも元の前髪を下地に二重合成せず、PSDの裏側を合成した静止下地を使う。
- 現在の調整を591c7ce973664430b4a3eeec378e7e05へ保存。repair_front_sourceで前髪p013だけPSD由来に置換し、p014〜p017の旧補助表示をOFFにした。元のSVGが未編集で座標・署名が一致することを検証してから処理する。元のPSD、元の保存は保持。
- forehair-upgrade-verifyで他の素材・モーション・目口補正・前髪の調整値・見せる強さ100%の保持を確認。PNG署名を正規化し、SVGの出力形も同じPSDから生成。修正版83299f2f3a844295a15b2e6d353ccaddを作成してユーザーのタブで開いた。
- Python56件成功。分離ON/OFF両方でPSD前髪のRGBA維持、固定の目元補助レイヤーが生成されないことを検証。実画面で0%・43%・100%を確認し、髪に固定して残っていた睫毛の影の解消を目視確認。顔下地の灰色の補修や閉じ目素材に元からある上側の線は今回の変更対象外。
- サーバーを再起動（PID10192）し、新規読み込み処理にも反映。ユーザー画面は比較しやすい43%で停止。

## 2026-09-09 今の素材の目元下地を補修

- 現在の作業を58464d26cc3d4c7184b98b2978e9c180へ保存。See-Throughの同じPSDから抽出済みのface素材を使い、core p001の目元14144画素の範囲だけ補修。未編集SVGの署名・元の座標を照合し、元ファイルや保存は上書きしない。
- eye_plate.blend_eye_skinは周囲の正常な肌からRGB差の中央値を求め、顔素材の色を合わせる。元の目の除去範囲を完全に置き換え、外周12pxを滑らかに混合する。前髪が重なる部分や半透明の輪郭を除外し、alphaと範囲外の画素を厳密に保持する。正常な肌の参照が不足する場合は変更しない。
- Python59件成功。新規3件は灰色の塗り跡の除去と色合わせ、範囲外・alpha・入力の保持、参照素材不足時の無変更、前髪領域の保護を検証。
- eye-plate-verifyで開き目・43%・閉じ目・目を隠した下地を比較。灰色のにじみの減少と貼り替え境界の改善を確認。全設定、core以外の全パーツ、coreの位置・大きさ・支点を保持し、PNG表示とSVGへの補修反映を確認。
- 正規化した修正版feeaee54fdba492abc0f00270fecfe2aを別保存し、ユーザーの編集画面で開いて100%の閉じ目にした。閉じ目PSDに元からある上の細い線は維持。今回はこの素材の保存を補修する試行であり、新規読み込みへの自動適用は追加していない。

## 2026-09-09 単独PSDの試用版

- open_a.psdを通常の/api/importへ1ファイルだけ送信。balanced・線整理ON・alpha12・開き目開き口として変換し、961c8967b22b4e1087afa3f08aa37bd9（29パーツ）を作成。元画像・追加の閉じ目/閉じ口PSD・手動補修の素材は使用していない。
- APIの戻り値で両睫毛と口の3つに生成した閉じ形があり、closedSource=psdのパーツがないことを確認。顔下地はPSD自身のfaceレイヤー。実画面で開き目・閉じ目・閉じ口を確認し、UIの「元の絵から作った下書き」案内も確認。
- 「単独PSD · open_a（閉じ形は自動生成）」として131e286dd9334570b764402522892872へ保存してユーザー画面で開いた。PSDの各レイヤーをSVG化する通常経路なので、元絵を使うHybridとは色や描画の細かさが異なる。今回ソフトウェアの変更は行っていない。
- 比較元で直近に行った口の調整は50c10555be27465fad18207e8db85e91へ保存済み。

### 「単独PSD」の意図の訂正：元画像＋PSD1枚

- ユーザーの意図は元画像＋分離PSD1枚で追加の閉じ形PSDなしだった。元画像source.pngとopen_a.psdだけを/api/import/hybridへ送信し、9042ae43a9c14123a1284ee6ceceb393（14パーツ）を新規作成。閉じ形3つが生成され、closedSource=psdのパーツがないことを確認。
- 元画像の色を使うHybrid・前髪はPSD素材・腕/髪の分離ONで、開き目・閉じ目・閉じ口を実画面で確認。730a6c62ac8d449fadaa9ce02558bfeaへ「元画像＋PSD1枚（追加差分なし）」として保存してユーザー画面で開いた。
- 通常の新規読み込み結果を試す版。前の保存に個別適用した目元の下地補修は、自動読み込みにはまだ組み込まれていないため、この版には未適用。

## 2026-09-09 アプリの通常読み込みに目元補修を組み込み

- これまでの個別補修だけでは新規読み込みへ反映されない問題を修正。convert_hybridがベースPSDの明示的なface/顔/顔の下地/顔下地レイヤーを読み、目パーツの除去範囲に色合わせと境界混合を実行する。追加の閉じ形PSDや過去の補修PNGには依存しない。前景の髪と透明度を保護し、分離ON/OFF両方で動作。顔や正常な肌の参照が不足すれば既存処理を維持。
- 補修済みPNGから通常どおりSVG・再構成画像を作る。conversionにeyePlateRepairと画素数を記録し、UIの読み込み後状態欄に自動補修したことを表示。既存保存の画素は勝手に書き換えない。repair_eye_plateも特定の旧プロジェクト/抽出PNGへの依存を外し、明示されたPSDから同じ入力生成関数を使うようにした。
- Python61件成功。閉じ形PSDなしで分離ON/OFFとも補修、色合わせ、顔レイヤーなしのフォールバックを追加検証。サーバーを更新してPID9296で起動。
- 実素材の元画像＋open_a.psdだけを通常の/api/import/hybridへ送信し、ab7c70e6c0ba40289a18002b591ed5dbを生成。アプリがeyePlateRepair=psd-skin-color-match-v1、14144画素の補修範囲を出力し、追加PSD由来の閉じ形がないことを確認。後処理の補修スクリプトはこの新規変換へ実行していない。
- アプリ出力のcoreで12761画素が変更され、alphaと補修範囲外は旧版と同一。直前のユーザー調整83a85efb77d24651b2ac362941e4277dへそのcore素材を引き継ぎ、12b1c3b53ae4450d9810113641f77cb1として別保存。目・口の補正とモーション設定は保持。eye-plate-verifyと実画面で開き目/途中/閉じ目/下地のみを比較し、にじみの減少と自動補修の状態表示を確認。

## 2026-09-09 症状別の補正カードから作業エリアの直接編集へ

- 右側の位置・大きさ・傾き・カーブを選ぶカードを廃止。作業エリア上部に部位、移動/変形、カーブ、Undo/Redo、完了をまとめた。目の確認中も左右の目にクリック/キーボード対応の選択領域を置き、選んだ側の閉じ形を78%重ね表示で編集する。余白のドラッグは移動、ホイール拡大も維持。
- 口も絵をクリックすると閉じ口6%の重ね表示で編集開始。完了で開閉確認へ戻り、モーション工程では編集ツールを隠す。右側の数値補正とリセットは維持。曲線編集に非対応のPSD差分ではカーブを隠し、左右選択が変わるたび対応を確認する。
- Node87件成功。direct-shape-verify16項目（目の選択、枠のドラッグ、Undo/Redo、カーブ、ホイール、口クリック、工程切替）、import-flow-verify26項目、work-area-verify17項目が成功。実画面でもクリックから編集枠表示、上部ツールの配置を確認。
- ユーザーの最新の揺れ・目口の調整をe6a4edc42bbd4ab286454ed9f68ef55cへ保存し、更新版で開いた。形の値は変更せず、画面左の目を選択して編集枠を表示している。

## 2026-09-09 モーション画面で口パクが開いたままになる問題

- 目/口の編集で使う口の開き100%がcurrentPoseのMath.maxによってモーションにも適用されていた。編集用スライダーを通常のループ姿勢に加算する処理を廃止し、口形プレビューの上書きは編集器が動作中の場合だけに限定。編集工程を離れるとプレビュー/母音/目の固定を解除する。口パクON/OFF、再生/停止、音声・カメラの入力優先は維持。
- Node87件成功。motion-mouth-verify10項目成功。編集からモーションへの移動、開口時刻のCanvas変化、閉口時刻の画素一致、OFF時の閉口、口編集から戻った後の同じ挙動、時間再生を検証。
- 最新の頭の傾き5.8等を7cec6c81ebba4b4ea2ce1e9d6d70fe4aへ保存して更新版で開き、モーション画面・ループ口パクON・再生中にした。実画面でも編集値0への解除と閉口を確認。

## 2026-09-09 工程を上部タブへ移動

- 1〜5の工程を編集画面上部の横並びタブへ移動。「形を整える」の左側は常時表示のレイヤー一覧とし、折り畳みを廃止。他の工程は左欄を隠して中央プレビューに幅を割り当てる。左右矢印/Home/Endによるタブ切替と選択状態も実装。
- workspace-ui.jsのNode構文チェック成功。実ブラウザ（幅1243px）で全5工程の切替、キーボード操作、14レイヤー表示、前髪選択からパーツ編集への遷移を確認。横にはみ出しなし。中央欄は編集685pxからモーション909pxへ拡張し、口パクONも保持。
- 更新前のユーザー調整を4e8791002273498eac635c5aa8712785へ別保存し、更新後の「形を整える」で開いた。停止していたサーバーをPID25940で起動。

## 2026-09-09 左のレイヤー一覧でドラッグ並べ替え

- 行全体をつかんで上下に移動し、挿入線の位置で離すとproject.partsの描画順を変更。上が手前。ドラッグ中は描画を作り直さず、ドロップ時に選択とプレビューを更新。一覧のスクロール位置を保持し、端の自動スクロール、Esc/領域外/ポインター中断でのキャンセル、Alt＋上下キーを追加。表示チェックと通常の選択を分離。
- app.js/layer-sort.js構文チェック成功。layer-sort-verify18項目成功（複数行の移動、戻す操作、描画グループの順序、属性保持、挿入線、キャンセル、表示チェック、自動スクロール等）。実マウスでも前髪を3行目へドラッグし、先頭へ戻ることを確認。
- 最新調整を341b839111734c29be662848d6dfab5aへ別保存し、更新版で開いた。実操作の検証後は元の順序へ戻し、形を整える・目の画面を表示。


## 2026-09-13 PSD neck and arm seams

- Kept separated PSD neck pixels beneath Face; removed the dilated face cut that erased the visible chin/neck boundary.
- Restricted original-image torso pixels to actual PSD core support so old arm/hair outlines and white gaps cannot remain as stationary fragments.
- Removed disconnected faint export noise from PSD motion layers while retaining connected antialiasing.
- Passed 28 Python checks across test_segmented, test_mouths, test_hybrid and test_eye_plate, including new neck coverage, residual-arm-outline, and faint-noise regressions.
- Reconverted the reported ComfyUI input as data/projects/7124789897f145dfa45d677220cff459 (original inputs and earlier edits retained).


## 2026-09-13 Independent tail motion

- PSD tail/尻尾 is a separate rear layer with the attachment pivot inferred from torso overlap. The flattened tail is excluded from torso artwork.
- Shared part motion rotates the tail about its pivot, with tailSwing (0–30 degrees) and tailCycles (1–4 per loop); the UI adds amount, speed and on-canvas pivot selection under 髪・腕など. Zero stops the tail.
- Tail is excluded from face orientation, retains full mesh coverage at maximum swing, and its role/pivot/settings survive project validation and package serialization.
- Passed all 179 Node tests and 29 Python checks (test_segmented, test_hybrid, test_control). Added pivot/loop/zero-amplitude and PSD separation tests.
- Actual app verified at 8 and 30 degrees, including pivot mode; screenshot qa/tail-motion-controls.png. Reconverted reported PSD: data/projects/6aa374a8c6f84b4c880ac3fb53bcade6, named original · Hybrid（尻尾の揺れ対応）, six-second loop.


## 2026-09-13 Optional See-Through secondary parts

- Preserve nonempty bottomwear, neckwear, earwear and wings PSD surfaces independently of the torso. Disconnected left/right earrings receive separate roots; faint empty-class export noise is discarded. Original uploads and existing conversions remain intact.
- Added optional per-part motion controls under 髪・腕など: rooted cloth hem (adjustable lower range), ribbon/earring/ornament pendulum, wing opening, paired speech-linked brows. Defaults are OFF; existing ear motion takes priority on an ornament assigned as an ear. Pivot picker is reused.
- `parts[].secondaryMotion` stores enabled/amount/cycles/range and survives project validation, save-state tracking, and portable playbackScene packaging. Canvas preview and frame/video/MPNG exports share the motion implementation. SpriTalk still requires the documented receiver adapter and exact matching runtime build.
- Node: 183 tests passed (`qa/secondary-node-tests.txt`). Python: 31 tests passed across test_segmented, test_hybrid, test_control. Regression cases cover torso isolation, empty classes, separate earring roots, fixed attachments, OFF, loop endpoints, speech-linked eyebrows, and package preservation.
- Browser raster checks: `web/qa-secondary-motion.html` confirms five motion types move, stop when disabled, and close the loop; endpoint raster tolerance is at most 1/255 per channel and total channel difference <=100 (GPU antialias rounding). Actual results: max 1, total 0–23.
- Actual UI: imported 86c236ee273c42c2b5c0546e6238ea5a (20 parts); verified optional controls, cloth enable/amount, live pivot picker and cancellation, paired brow settings (40% copied to other brow), contextual hidden controls. Screenshot: `qa/secondary-motion-controls.png`. Missing wing/ribbon/earring artwork was tested with synthetic rendering/import fixtures rather than claimed as character-specific visual validation.


## 2026-09-13 Closed-mouth transform isolation

- Removed closed-art X/Y/angle offsets from mouthOpenMotion. Closed artwork retains its own transform; the compressed source mouth and vowel artwork retain only their open/vowel transforms. The existing opacity crossfade is unchanged.
- 23 mouth-controls, mouth-gizmo, closed-mouth and motion tests passed. New regression samples opening 0, 1, 4, 8, 12, 18, 50 and 100 percent with vowels both on and off; changing closed X/Y/angle never changes the source-mouth Canvas matrix or SVG preview channels.
- Actual workbench checked at 6% opening: drag (+20,-10 screen px) moved the closed handle by exactly that amount, while source mouth channels remained translate(0 0) and rotate(0 519.5 174). Reverted the verification drag with Undo; original user tab left untouched. Screenshot at 0%: qa/closed-mouth-transform-isolated.png. Existing open tabs need a reload after saving edits to load the new module.


## 2026-09-13 Live secondary controls and motion categories

- Confirmed the affected user screen had eyeThroughHair=true and playback running. Cached eye-through parts held the previous secondaryMotion object when the controls replaced it. Passes now read the live source property, preserving cached textures without rebuilding on slider input. Initial ON, amount, cycles, range, OFF and reset are covered. Secondary control edits also resume playback in motion mode.
- Organized existing controls into 身体 (breath/body/chest/bounce), 顔・瞳 (blink/talk/XYZ/iris/brows), 髪・腕, 服・小物, 耳・尻尾・翼. Category navigation stays visible while scrolling; switching starts at the top. Brow targets are combined and moved out of the clothing list; absent clothes show an explanatory message. Existing control IDs and saved settings retained.
- Node suite: 185 passing (qa/motion-live-category-tests.txt). Browser fixture web/qa-secondary-motion.html now tests first-time ON and replaced OFF after eye-through renderer preparation across cloth, wings, neckwear, earwear and brows. All passed; no renderer recreation.
- Actual UI: imported 86c236ee273c42c2b5c0546e6238ea5a, enabled eye-through, edited cloth to 65% / 2 cycles / lower 35%, checked playback resumes, no loading state, category membership and no duplicate IDs. Original user tab remains untouched. Screenshot: qa/motion-categories-live.png.
