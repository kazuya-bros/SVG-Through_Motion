{
  "identifier": "desktop-settings",
  "description": "Only the bundled settings window can control the desktop process. No remote origin receives native permissions.",
  "windows": ["desktop"],
  "permissions": ["core:default"]
}
