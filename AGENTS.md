# エージェント制御の製品方針

- 主要な制作・補正・出力機能は、AIエージェントがAPI / MCPで状態取得・実行・結果確認できる設計を基本とする。UIとMCPは共通の処理を使う。
- 新機能では引数検証、進捗・失敗、競合・再送、確認画像や成果物の取得を検討する。画面専用の操作は明記する。対応範囲・接続は `docs/agent-control.md` を参照する。

# UI変更時の確認

- ユーザーの希望により、画面を変更したときは変更後の実画面のスクリーンショットを撮影し、最終回答に画像として添付する。
- モックアップではなく、実装した画面を撮影する。

# メインメニューへの移動

- 制作・素材選択・起動前の画面でメインメニューへ戻る操作は、左上のロゴに統一する。画面内に同じ用途の戻るリンクやボタンを追加しない。
- 編集途中にロゴを押した場合の保存確認は維持する。工程内の「戻る」「キャンセル」は別の操作として扱う。

- 出力ウィンドウにはロゴやメインメニューへのリンクを置かない。背景は開く前の画面で選び、出力ウィンドウに背景設定を重複させない。

- 配信ソフト向けの表示URLは出力ウィンドウごとに異なるため、取り込み案内とコピー操作を各出力ウィンドウに置く。起動前の画面には重複させない。
