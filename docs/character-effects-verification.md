# 演出の検証（2026-09-15）

## 自動検証

- JavaScript全211件、Python全131件が成功。共有時刻での進行、消滅後の非表示、再登場、解除、発話終了による復元、置き換え、重複防止、切断とタイムアウト、出力側からの描画結果通知を含む。
- 最終調整後、演出のJavaScript4件を再実行して成功。
- 実MCPのstdio接続で同梱exeから8ツールを検出し、演出スキーマ・5種のpreset（noneを含む）を取得。PNG登録→プレビュー画像→PSD書き出しも成功。
- 同梱バックエンドの起動、認証、PNGからSVGへの実変換、親stdin終了に伴う停止が成功。
- TauriのWindowsリリースビルドが成功。

## 実画面

隔離サーバー18767、Tethサンプルのコピーを使用。マイク・カメラは有効化していない。出力の音声有効化ボタンは押したが、発話は行っていない。

- 通信中: UIクリックとAPI開始の両方を確認。緑の色調と走査線を描画し、API指定の強さ・時間がUIにも反映。
- 幸せの光: 顔の周囲の暖色の光と星を確認。
- 消滅: 粒を伴って消え、終了後の非表示を確認。UIの「粒から現れる」で再登場し、途中の停止でも通常表示に復元。
- 配信向け表示: 通信演出の途中に開き、進行中の見た目に合流。消滅も両画面で同じ段階になり、操作UIは配信表示に出ない。
- 切断: 出力元を閉じると配信表示のキャラが消えることを確認。検証用タブと検証サーバーは終了済み。
- スクリーンショット: `qa/effects-comms-20260915.png`、`qa/effects-happy-20260915.png`、`qa/effects-dissolve-20260915.png`。

## 配布物

`output/desktop/SVG-Through-Motion-0.2.0-windows-x64-20260915-193545.zip`

118,162,238 bytes、1,187ファイル。ZIPのCRC検査成功。4つの演出関連JavaScriptが最終ソースと一致。既存配布物は保持。

## 未検証の範囲

外部TTSの実発話との同時動作、実カメラ追従との同時動作、OBSアプリ自体での取り込み、WebView2上での演出の実画面、OSの動きを減らす設定を切り替えた実画面は未検証。発話連動の状態遷移と動きを減らす設定値の伝達は自動テストで確認。画面確認は実装を配信する内蔵ブラウザーで行った。
