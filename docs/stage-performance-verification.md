# AIと演出の検証（2026-09-15）

## 自動テスト

- JavaScript全217件成功。演技の時間遷移、画像クロスフェード、CJK・絵文字の改行、口パクを保ったポーズ加算、画像準備と描画後の通知、古い読込完了の無視、発話終了時の復元を含む。
- Python全136件成功。MCPのstdioパイプはサンドボックスのWindowsアクセス拒否が出たため、該当1件を通常権限で再実行して成功。
- 画像形式・サイズ制限、登録の重複防止、保存済みプリセットの再取得、設定競合409、AIモードの確認、実WebSocketの通知、配信側からの偽の完了通知の無視、タイムアウト・切断後の再生抑止、確認画像の往復を検証。
- 最終調整後に演技JavaScript6件・Python5件を再実行して成功。

## 実画面と実MCP

隔離サーバー18768とTethサンプルのコピーを使用。通常の制作データと通常サーバーは変更しない。画像は透明な月・雲・星を描いた検証用フィクスチャで、画像生成サービスの出力ではない。

- ソース版MCPの実stdio接続で画像登録、レシピ作成・保存、再生、running確認、出力のPNG取得まで成功。
- 出力の実画面で画像・字幕・明朝体・文字色・表情の変化を確認。取得画像を見て、頭付近の画像を顔に重ならない位置へ修正。
- 画面から通常の字幕文・サイズ・色を更新。2工程の演技を再生し、終了すると通常字幕へ戻ることを確認。
- 「実行中の演技を編集欄に取り込む」と、画面からの新規プリセット保存を確認。
- ブラウザー音声で短い検証文を実際に再生し、自動字幕・口パク・画像・光を確認。発話completed後、発話連動の演技はinterruptedとなり、通常設定へ復元。
- 配信向け表示を途中で開き、画像・字幕と進行中の演技を確認。操作パネルは配信表示に出ない。
- 「通常に戻す」でAIモードOFFを確認。

スクリーンショット:

- `qa/stage-ai-ui.png`：最終UI、配置画像、字幕と編集項目。
- `qa/stage-obs.png`：配信向け表示。
- `qa/stage-mcp-preview.png`：MCPが返した実描画画像（配置調整前）。
- `qa/stage-speech-preview.png`：実発話中の自動字幕・画像・光。

## Windows配布物

`output/desktop/SVG-Through-Motion-0.2.0-windows-x64-20260915-213211.zip`

118,191,945 bytes、1,195ファイル。ZIPのCRC検査と、同梱した最終player/stage JavaScript・CSSの一致確認が成功。バックエンド起動・認証・PNG→SVG変換・親stdin終了時の停止が成功。同梱exeのMCP経由でも演出画像登録・プレビュー・レシピ保存と取得、既存素材ワークフローが成功。

## 検証の範囲

画面確認は内蔵ブラウザーを利用。WebView2上での実画面、OBSアプリ本体、外部TTS、実カメラとの同時使用、第三者LLMホスト個別の設定、GPT Image等の外部生成サービス呼び出しは未検証。MCPで登録できるのは生成済みの画像ファイル。生成サービスをアプリへ直接接続する変更ではない。
