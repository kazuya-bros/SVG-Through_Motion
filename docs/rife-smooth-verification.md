# 8枚SVGの連続補間・終端補正（2026-09-17）

## 変更

- 最近傍コマの切り替えを廃止し、隣接2コマの上下位置と高さを合わせながら連続ブレンドする。
- Canvasは分離した局所バッファで加算合成、SVGは分離グループのplus-lighterで合成。半透明を通常の重ね描きにした際のアルファ低下を防ぐ。
- 生成時には33点の等間隔、粗い推定点、端点近傍を測定。透過込みの画像の変化量に応じて8枚の採取位置を選ぶ。
- 旧8枚プロジェクトも連続ブレンドで再生する。上下の位置合わせとコマ配分の改善には再生成が必要。

## 検証

- JavaScript 241件、関連Python 28件成功。
- 全キーと0/1は重み1・変形なし。すべてのキー境界で重みが連続し、2枚の上下端が一致することを検証。
- 人工的に終端へ偏る変化（tの8乗）の採取後、7区間の変化量の最大/最小比が1.05未満。
- EdgeでSVGを画像として描画し、赤と青を0.5ずつ合成したピクセルがRGBA=(128,0,128,255)になることを確認。
- 保存済み20パーツのキャラクターで目・口を生成・API適用・保存・再読み込み。3対象ともgenerated/current/enabled=true、各8枚。
- 実素材の左目を幅256pxで測定した7→8枚目の高さ差は16pxから5pxへ減少。これはこの素材での値であり、任意の素材への保証ではない。
- キャッシュ済みCanvasの開閉量を0〜1まで0.01刻みで描画し、全区間が変化することを確認。色をアルファで重み付けした画素差の平均は約50,653、最大約57,489、最終区間約53,099。終端だけの大きな跳びは観測しなかった。
- 目・口だけの描画時間は120回平均約0.069ms。全身・リグ・初回読み込みの性能ではない。
- 実装UIの確認画像: `output/verification/rife-smooth-20260917.png`。

上下の位置合わせは線や瞳を個別に対応付けるものではないため、二重線やぼけが残る場合がある。新しい生成は測定回数が増えるが、再生時のRIFE実行と毎フレームのSVG変換は不要。
