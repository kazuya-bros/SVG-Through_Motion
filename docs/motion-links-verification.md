# 動きの連結の検証（2026-09-17）

- JavaScript 246件成功。裾と胴体・脚・腰巻が同一座標で同じ変形・メッシュを使うこと、上側固定、裾より下の移動、元OFF、解除、顔・頭のリグ維持を確認。
- 関連Python 15件成功。APIの必須引数、ID、重複対象、変更前状態の指定を検証。
- 共通処理は循環・不明ID・素材追従との重複を更新前に拒否する。複数対象の失敗で一部だけが更新されないこと、同一状態の再適用がchanged=falseになることを確認。
- 20パーツの実キャラクターで服の裾（p008）と胴体・脚を含む元画像（p006）をUIから連結。
- APIでinspect→unlink→linkを実行。古いexpected値での解除がfailedになることを確認。
- 顔リグをOFFにしたCanvasでも、連結の有無により胴体の画像が変化した。RGBAの画素差合計4,731,087。
- プロジェクトJSONを保存して再読み込みし、p006の連結チェックがONのままであることを確認。
- 配布用にビルドしたバックエンドでも、UI・API・描画差分・保存後の再読み込みの同じ確認が成功。
- 実装画面をヘッドレスEdgeで撮影。ユーザーのTauriウィンドウは操作していない。
- 画像: `output/verification/motion-links-20260917.png`。

連結で共有するのは基準の裾・翼の追加変形。元のリグや別の動きをすべて置き換えるものではなく、素材の位置・重なりや別の動きに由来する差は残る。
