# 自動睫毛の編集（2026-09-19）

- 黒ずみ：目の編集用 `eye-hit` に18pxの半透明ストロークがあり、未選択側の睫毛に灰色の帯が重なっていた。ヒットテスト用のストロークは透明にし、選択枠内の塗りも透明に変更。クリック・ドラッグの対象は維持。
- 目の手動補正の初期表示を100%閉じに変更。78%の開き目との重ね合わせは「開いた形と重ねる」で明示的に選択できる。
- 自動生成の輪郭に色・量・3種のプリセットを追加。PSD差分を除外し、元SVGは不変。量0で突起なし、既定で元の突起、最大で追加の睫毛も表示。量は連続的に変化する。
- 色は元色・茶・グレー寄りの黒・任意の色。入力を6桁のHEXに制限し、量は0〜2に制限。
- 既存のlidAdjust、Undo/Redo、キャンセル、プロジェクト保存、RIFE署名検証を共用。

実データの自動生成した閉じ目を独立した18787のアプリで確認。ユーザーの18765へのアクセスはプロジェクト・素材のGETのみ。保存の検証は隔離したデータフォルダで実施。

検証結果：Nodeテスト259件成功。実画面で透明ヒット領域の左右切替、色変更によるキャンバス画素の変化、Undo、キャンセル、プリセット、プロジェクト保存と再読み込みでの色・量の保持を確認。画面例外なし。実画面は `output/verification/lash-style-final.png`。


## 本数・閉じ口・連動先の整理（2026-09-19 追補）

- 睫毛の形プリセットを非表示にし、0〜16本の整数スライダーに統一。元の形は本数未指定時に保持。旧lashAmountの保存データは互換処理で再現する。
- RIFEタイトルから「試用」を削除。
- 閉じ口の先細り・カーブ・左右口角を通常のスライダーとして表示。対応する生成輪郭がある場合のみ有効。
- その他の調整では役割・不透明度・個別描画方式・支点指定を隠し、連動先と重なり順を中心に表示。
- 連動先UIはeditor.commandのmotion_linksと共通。attachment方式、既存followPartの変更・解除、競合検知、循環拒否を検証。

Node 263件、Python control 11件成功。実データを隔離サーバー18787で操作し、睫毛8本、先細り30%、尻尾→裾、眼鏡→Faceの保存・再読み込みとモーション画面の例外なしを確認。18765は読み取りのみ。実画面はparts-refine-eyes.png / parts-refine-mouth.png / parts-refine-link.png。再ビルドしたバックエンドのPNG/PSD変換・親プロセス終了処理も成功。
