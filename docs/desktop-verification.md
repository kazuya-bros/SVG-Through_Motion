# Tauriアプリ化の検証 — 2026-09-15

## 基準と構成

- アプリ化前の復帰点: `d262913`。着手時の作業ツリーはcleanで、追加の未コミット差分はなかった。
- Tauri 2 / Windows x64 / WebView2。通常処理用Python 3.10と依存をPyInstallerで同梱。
- 制作画面、素材準備画面、出力画面は既存HTTP・WebSocketを利用。
- アプリの専用ローカルサーバーは18765。既存8765サーバーを変更・停止しない。
- 検証用設定: `.desktop-build/desktop-settings.json`。ユーザーの通常アプリ設定とは分離。

## 自動検証

- JavaScript 207件成功。表情を重ねて押す、異なる順に離す、押し続けによる重複入力、
  通常選択への復帰、ローカルキーとネイティブキーの独立した解除を含む。
- Python 122件成功。新規のデスクトップAPIの認証、外部Origin拒否、指定セッションへの
  表情送信、存在しない表情・未接続出力の拒否を含む。
- Rust 2件成功。キー名の別表記による重複、修飾キー、表情番号、切替方式の検証。
- `cargo check` と `tauri build --no-bundle` 成功。
- `tools/verify_desktop_backend.py` で同梱EXEを単独起動。
  認証済みhealth、HTML/JS配信、PNGからVTracerでの変換、指定dataへの保存、
  親stdinのEOFによるサーバー正常終了を確認。

## 実アプリの画面・操作

- アプリ設定→制作メインメニュー→「キャラクターを使う」→ファイル選択ダイアログから
  保存済みの `Teth2 · 統合動作確認` を読込→カメラ用出力ウィンドウを開く。
  カメラ開始ボタンは押していない。
- 出力セッション `0322d5cc1ea84d07b74e7f56d212d944` でSVG描画を確認。
- アプリ設定UIで操作対象を選び、Ctrl+Alt+1 / 2 / 5をOSへ登録。設定ファイルへの保存を確認。
- 出力以外のアプリ設定ウィンドウでCtrl+Alt+2を押し、出力が笑顔へ変化したことを確認。
- OBSと同じdisplay WebSocketで425フレームを受信。左右の目が0.35、眉が0.2となる
  表情適用後の値を確認。`tools/observe_desktop_pose.py` を使用。
- Ctrl+Alt+5の押下・解除で二つのAPI送信成功と一時表情解除の表示を確認。
  自動操作の短いキー押下ではウインク中のフレームは採取できていない。
- アプリ終了確認から終了し、出力接続の切断とPythonの正常終了をログで確認。
- 実画面で見つかったWebView2の複数ウィンドウ起動問題を修正。
  全ウィンドウで同じブラウザ引数を使い、バックグラウンド描画の抑制を減らした。

## 実画面の画像

- `qa/tauri-main-menu-20260915.png`
- `qa/tauri-global-hotkeys-20260915.png`
- `qa/tauri-expression-output-20260915.png`

## 検証の範囲

配布物は `output/desktop/SVG-Through-Motion-0.2.0-windows-x64.zip`。
ポータブル版にはアプリ本体、通常処理のPython、使用ライブラリのライセンス、使い方を同梱。
ユーザーの `data` やSee-Throughモデルは含めない。

- Explorerへフォーカスを移す検証は操作許可の時間切れで未実施。
  OSへの登録と、出力以外のウィンドウからのネイティブイベント受信は確認済み。
- 実ゲーム・OBSアプリ自体を前面にしたキー操作、実カメラ・マイクの入力、
  他PCのクリーン環境、インストーラー実行は今回の確認範囲に含めない。
- 本物のOS競合を意図的に作る検証は未実施。登録失敗をUIへ返す実装と、
  キーの重複拒否テストを備える。
- See-Through環境・GPUモデル・外部TTSは既存の準備済み環境を使用する。
