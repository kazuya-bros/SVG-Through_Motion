# API / MCP改修の検証（2026-09-15）

- Python: 128件成功。全42操作のOpenAPI解決、素材アップロード→補正→競合409→PNG確認→PSD保存、フォルダー/URL制限、表情の適用返信・重複防止・切断/タイムアウトを含む。
- JavaScript: 207件成功。既存の描画・表情・入力・補正機能の回帰検証。
- MCP: 実際のstdioクライアントから8ツールを検出。隔離HTTPサーバーへPNG登録、状態取得、MCP画像ブロック取得、PSD書き出しとダウンロード。ソース版と同梱exeで成功。読み取りツールへの変更操作は理由付きエラー。
- 同梱バックエンド: 起動、ネイティブ認証の維持、PNG→SVGの実変換、親stdin終了による停止が成功。
- 実画面: 隔離サーバー18766でTeth2のコピーを出力。表情APIでウインク/強さ0.8がcompletedになり画面にも反映。画面の笑顔クリック後にAPI状態index=1/strength=0.8を確認。音声有効化・カメラ/マイクの許可は操作していない。
- UI: メインメニューのガイドリンク、接続案内を撮影。`qa/agent-main-menu-20260915.png`、`qa/agent-mcp-guide-20260915.png`、`qa/agent-expression-api-20260915.png`。
- Windowsポータブル版を再ビルド。既存0.2.0配布物を残し、`output/desktop/SVG-Through-Motion-0.2.0-windows-x64-20260915-182857`に更新版を保存。

今回、実GPUでの分割/Depth生成、外部TTSによる発話、全編集操作のMCP経由再実行、外部MCPホスト製品ごとの設定は検証していない。これらは既存APIとスキーマを利用する。ウィンドウ起動・OS許可・一部入力設定の画面依存は残る。
