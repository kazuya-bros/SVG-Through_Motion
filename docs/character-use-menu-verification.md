# キャラクター選択・ホットキー導線の確認（2026-09-16）

- 「キャラクターを使う」のプレビューを360×390pxでプルダウン上中央に配置し、「編集する」を削除。狭い画面では横幅に合わせて縮小。以後保存するサムネイルも480px幅に拡大。
- 全ウィンドウのメニューバーを非表示。表示ウィンドウの右側操作パネル上部に「ホットキー設定」「アプリ設定」をまとめて表示。
- ホットキー設定では保存先・接続設定を隠し、呼び出した出力を選択。適用ボタンを押すまでは実際の操作対象を変更しない。
- ユーザーと相談の結果、ファイル読み込みはこの「キャラクターを使う」画面に置くことで確定。編集画面を経由せず利用できる。

## 検証

- JS構文確認、Rust既存テスト3件、Tauri releaseビルド成功。
- 別identifier・別設定・ポート18771の検証アプリで、制作メニュー／出力メニュー／設定対象の自動選択を実機確認。
- 元素材のない新規保存先へ、既存のアプリ保存JSON（20パーツ、53件の埋込画像）だけをファイル選択から読み込み、プレビューと出力の復元を確認。projects/uploadsはともに空。出力APIのconnected=true、error=null。音声の有効化待ちなのでready=false。今回は発話を行っていない。
- JSONには元PSDのレイヤー編集情報を含まない。素材補正からの再開には元PSDか素材作業一式ZIPが必要。音声エンジンも別途必要。
- reconstructedUrlの内部パスが残るが、現行web描画処理は参照していない。
- ZIPのCRC検査と、release EXE・変更webファイルの一致を確認。

実画面: qa/character-use-menu.png、qa/output-hotkey-menu.png、qa/output-hotkey-settings.png、qa/portable-character-restored.png。
配布: output/desktop/SVG-Through-Motion-0.2.0-windows-x64-20260916-214534.zip。
検証用identifierのみが配布版と異なる。通常のユーザー設定と実行中アプリには手を加えていない。

## メニューバー非表示の追修正

制作画面・素材画面・設定画面・出力画面すべてでメニューを付けない。出力の設定ボタンは同じアプリ内の出力ページからの専用ナビゲーションとして処理し、ページ遷移せず設定ウィンドウを開く。HTTP APIやネイティブコマンド権限は追加していない。制作ウィンドウ右上の×による既存の終了確認は維持。ユーザー指定により、この追修正の実機確認・撮影は省略。

追加検証: Rust 5件成功（呼び出し元セッションの選択、別Origin・別画面・配信表示・不正引数の拒否を含む）。player-controls.js構文確認。最新変更の実機確認はユーザー指定で省略。
